# SPEC — Kladra

Kladra is the CRM and operations tool for Technopanel, a Riyadh company selling aluminium
composite panel (ACP) cladding. Fourteen people. It replaces FACET, whose interface users
rejected as overwhelming, record-first and slow. This file is the business (§2, extracted from
FACET's SPEC and its thirty archive documents at C:\Projects\facet-crm), the founder's decisions
(§3) and the defaults Claude chose (§4). How it looks is DESIGN.md.

## §1 People

- **Rep** — owns companies, logs what happened, requests quotations, raises dispatches, chases customers. Has a monthly m² target.
- **Marketing** — owns companies and works them exactly as a rep does — logs, follow-ups, projects — and hands each one to the rep who will price it. No quotations, no dispatches, no target.
- **Coordinator** — the desk: issues quotations in SMAC and types the number back, or sends a request back; approves or refuses dispatch requests. No target.
- **Manager** — sees every rep, every company, the team table and what is stuck. No personal target; the company target is his measure.
- **Admin** — users, passwords, targets, lookups, holidays, export. Sees what the manager sees.

Real team: Jerom (admin) · Abdulrahman Al-Zahrani (manager) · Rawan (coordinator) ·
Faisal Al-Harbi, Saad Al-Qahtani, Turki Al-Shammari (reps). Marketing is a role of its own
from P8 (D50); the seed carries it as an account, not a named person, until Jerom says who.

## §2 How the business works

**The company and its tools**
- S1 Technopanel is a Saudi supplier of cladding and aluminium composite panel. Riyadh is the main location; the Eastern and Southern provinces each have a rep who opens the local warehouse.
- S2 About fourteen people use the system and there is no IT team.
- S3 SMAC, the company's ERP, is the record for money: quotations, invoicing, finance, tax, stock. Kladra is the record of work: who owns which relationship, what was promised, what actually moved. It never takes on finance.
- S4 There is no integration with SMAC. Every SMAC number is typed by a person and can be wrong.
- S5 The unit that matters is the square metre, tracked monthly. Money is the finance department's concern.
- S6 English is the main working language; Arabic is available for those who find English hard. A company, contact or project has one name, in whichever script the rep prefers.
- S7 Nobody self-registers. Accounts are created by the admin and deactivated, never deleted, so history always points at a real person.

**Who sees what**
- S8 Reps see only their own companies; the manager sees everyone's. A manager who sells carries no personal target because his metres are already in the company total.
- S9 The coordinator runs both chains, quotations and dispatches, and deals with SMAC and finance. She does not own customer relationships.
- S10 Customers never use the system; whenever a step waits on the customer, the rep is responsible for chasing.

**Companies and contacts**
- S11 The customer is the company. The people at it are contacts. A contact belongs to one company; a person who moves gets a new contact at the new company, and the old one stays for history.
- S12 Companies arrive from a rep's own search, from marketing, a referral, an exhibition, online or WhatsApp. Lead source is how the founder learns where business actually comes from.
- S13 The rep who adds a company owns it.
- S14 The phone number is the strongest sign that two records are the same company: names vary, numbers rarely do. +966, 00966, 966 and a leading 0 are the same number.
- S15 A company is always created even when it looks like a duplicate; nothing blocks the rep.
- S16 Nothing is deleted. A company nobody works any more is archived: if it resurfaces in two years the record shows it was already known and why someone gave up on it.
- S17 Between one and two companies a day arrive in steady state, up to a few hundred a month after an import.

**Projects**
- S18 A project is a job at a customer, a tower, a villa, a mall, and every quotation belongs to one.
- S19 Expected m² is the rep's own estimate and the anchor number of a project. The pipeline total is the sum of expected m².
- S20 Lost is the rep's judgement, requires a reason, and closes the project. A rejected quotation is not a lost project; the project continues.
- S21 Won is never set by hand. A project is won when a dispatch against it is approved.
- S22 Cladding sales are project-driven with long cycles and many enquiries that never mature. Ten 50 m² quotations must not outweigh one 5,000 m² quotation.

**The log**
- S23 Reps work in the field and log a visit standing in a lobby with a phone. Logging must take under a minute, or it is filled in later from memory, which is a permanent guess.
- S24 A log entry is about one company, may name a contact and a project, records the channel (visit, call, WhatsApp) and what happened in the rep's own words.
- S25 Sales activities, catalogue sent, samples, documents, visits, calls, happen in any order and any number of times. They are not stages.
- S26 A rep is asked to write only what the system cannot see. If it can be known from an event, nobody is asked and no second copy is kept.
- S27 The history of a company is the manager's daily report. There is no separate report to write or submit.

**Quotations**
- S28 A rep raises a quotation request with all the lines. The coordinator creates the real quotation in SMAC and types its number back; that is when it is issued.
- S29 The coordinator can send a request back for edits, with a reason, and the rep who raised it is told.
- S30 The physical signature on the quotation is management's approval, price included. There is no separate price-approval step.
- S31 Prices are per m² in SAR. m² = pieces × width × length, never typed. VAT is 15%, fixed. Where Kladra's figures and SMAC's disagree, SMAC is right.
- S32 A line names supplier (N, K, C, D), class (A, B, A2G1, A2G2), fire rating (B1, A2, Normal), colour code, thickness, width, length and pieces. 4 mm is the standard thickness; a standard sheet is 1.24 × 5.8 m; coils are production's concern, not sales'.
- S33 Colour is a code such as 168, typed; occasionally a RAL or Pantone value.
- S34 A revision is a new quotation linked to the old one; SMAC puts RE before the number. Earlier versions stay readable; only the latest is live.
- S35 Quotations are never summed. A project quoted three times at 2,000 m² is 2,000, not 6,000.
- S36 The customer's answer belongs to the quotation: accepted, or rejected with a reason.

**Dispatches**
- S37 A dispatch is the customer's reply to a quotation. Cladding is taken in stages, so one quotation normally produces several partial dispatches; quoted, paid and dispatched quantities differ.
- S38 A dispatch may only be raised against an issued quotation.
- S39 The rep raises the dispatch request; the coordinator checks it against the quotation and approves it with the SMAC dispatch number, or refuses it with a reason.
- S40 Shipment is CT (the customer's own truck), TT (a Technopanel truck) or Cargo (a third party); the rep chooses.
- S41 Approval is final and is the only event that counts: not the request, not the number. If something goes wrong afterwards a new dispatch is raised.
- S42 Dispatch normally happens in the same month as the order; dispatching before the cladding ships is rare.

**Targets and counting**
- S43 Targets are m² per month per rep, set by the admin. Achievement is derived from approved dispatches; nobody types how much they did.
- S44 The company target is one figure per month set by the admin alone. It does not derive from the reps' targets and they do not derive from it.
- S45 A rep with no target still appears with all his real figures and a dash where the target would be.
- S46 Target progress and activity are shown side by side and never combined into one score. Nothing scores a rep.

**Calendar and follow-ups**
- S47 Friday and Saturday are the weekend for everyone; Saturday work is recorded, never required. A day is a Riyadh calendar day.
- S48 Public holidays such as Eid affect everyone; personal leave affects one person. Both are skipped by pace and reminders: a rep back from two weeks off must not be told he is behind.
- S49 Pace is working days done over working days in the month. In the first five working days it reads "the month has just started".
- S50 A next-follow-up date set by the rep silences chasing until it arrives and then becomes the reminder. Due today is due, not overdue.
- S51 A company added and never contacted reminds its rep after 14 days, and the manager sees a per-rep count, because adding forty companies and working none is a real habit the founder wants visible.
- S52 A reminder is cleared by doing the work, never by dismissing it.
- S53 A decision that ends someone's work, a request sent back or refused, a quotation rejected, reaches them with its written reason.

**Records**
- S54 Today reps and the coordinator negotiate quotations over WhatsApp, where the record vanishes. The aim is the outcome written on the record.
- S55 Every change is audit-logged with who, what, when and which record.
- S56 The pilot is two or three reps for a month before rollout. Open decisions are revisited after the first month, three months and a year of real data.
- S57 Success in six months: the manager sees pipeline, targets and activity without asking anyone to assemble anything, and no rep keeps a private spreadsheet.

## §3 Founder decisions from user testing

- Company has NO phone; phone is on the contact and mandatory there.
- Company form: Name · Category (Other last) · Lead source (Other last) · Country (Saudi Arabia default) · City (Riyadh default) · Notes. Contact captured in the same popup: Name · Phone · Position · Email · Notes.
- Lead sources: no Contractor, no Architect; Online and WhatsApp separate; Other last.
- Countries pinned: Saudi Arabia, UAE, Bahrain, Kuwait, Qatar, Oman; rest alphabetical; searchable. Cities pinned: Riyadh, Jeddah, Dammam, Khobar, Makkah, Madinah; rest alphabetical; searchable. Non-Saudi → city free text.
- Project: no state at creation; "Mark lost (reason)" is a later action. No in-production / committed flags.
- Next follow-up: date picker, shown at top, surfaces as due today / overdue on the rep's home. Date format everywhere 04/Aug/2026.
- Quotation lines are Item 1, Item 2… Columns: Colour code · Supplier (N/K/C/D) · Fire rating (B1/A2/Normal) · Class · Qty · Thickness · Width (1.24 / 1.5 / 2.0 / Other → number) · Length · Price per m². m² = width × length × qty. Line total, VAT 15% and grand total shown live. No internal codes shown, ever.
- Quotation raised from inside a company or a project, in a popup.
- Coordinator's quotation actions are exactly two: Issue (enter SMAC number) and Send back (reason). Customer accepted / rejected (reason) is the rep's action, on the rep's screen, after issue.
- Dispatch raised from an issued quotation, in a popup: pick items and quantities (partial allowed), shipment method, destination, payment terms written by the rep. Coordinator checks, then Approve (enter SMAC dispatch number) or Refuse (reason). Approved m² counts toward the rep's target in the month of approval.
- Manager has no personal target; sees the company target and everyone's achieved, his own included as team.
- Admin sets all targets, manages users, resets any password, edits lookups and holidays. Admin only can export.
- No comments feature. No refresh buttons: the screen updates itself.
- Sidebar collapsible. Searchable dropdowns. Date pickers. Loading states. Transitions. Toasts.
- Every phone number is stored normalized (E.164, +966…) and displayed local (05x xxx xxxx). Input accepts 05x, +966, 009665. Duplicate warning matches on normalized phone. (P3)
- Tapping a phone anywhere opens WhatsApp via wa.me; long-press/secondary shows the number. (P3)
- Ctrl+K / Cmd+K opens global search from any screen. (P2)
- The app is installable on a phone (PWA manifest, icons, offline splash only — no offline data). (P7)
- Sessions last 30 days; sign-out is explicit. (P2)
- Every empty list shows one sentence and its primary action. (all) — narrowed by D23 and D31, which say what happens when the action does not apply or does not exist yet.
- List filters and the open drawer are reflected in the URL. (P3+)
- Archive, never delete: companies, contacts, projects get archived_at; archived rows hide from lists and stay in history. Admin can restore. (P3)

Two proposals from P11J were put to him and he answered both. (P11J)

- **The desk is never empty.** A screen for "Rawan is away, who answers the queue" is not needed: there is a second sales coordinator, and the queue is a desk two people share rather than one person's inbox. Nothing is built; the manager is not given the coordinator's actions.
- **"Gone quiet" means one thing.** A separate screen for a customer who has lapsed is not wanted; the gone-quiet band on the rep's own list already says it, and a second name for the same silence would be the two-figures defect (rules/words.md). One meaning, one place.

Jerom used the app himself before P8. His verdict was that it beats FACET and is worth deepening. These add to the list above; none of them replaces anything in it. (P8)

- Every list creates from its own screen. Projects, quotations and dispatches had no primary action of their own and he had to go hunting through a company. Where the record needs a parent, the button asks for the parent instead of not existing. (P8)
- The side panels are reworked: what a person needs first is at the top, and a panel has a hierarchy rather than a flat list of fields. (P8)
- Square metres are the headline figure on quotations and dispatches. Price is the quiet supporting one, because SMAC owns money. (P8)
- Colour carries meaning: state, overdue, stuck, ahead of target, from one small set used the same way everywhere. Never colour for decoration, and never colour alone. (P8)
- A dashboard per role, each answering that person's daily question: the rep's day, the coordinator's queue, the manager's team. Not a generic grid of cards. (P8)
- The admin can view the app as any role or any person, clearly marked as viewing, to check and test it. (P8)
- Roles may grow beyond the four if the business needs them. (P8)
- Where a screen earns more than one view, the default is the one that answers the daily question, and the choice is remembered per person and carried in the URL. Which screens earn which views is decided in DESIGN §6, with the reason. (P8)

## §4 Defaults Claude chose — founder may change

- D1 Lead sources, in order: Field visit · Direct contact · Referral · Exhibition · Marketing · Online · WhatsApp · Other (FACET's list with "Online or WhatsApp" split and "Consultant or architect" dropped, per §3).
- D2 Categories, FACET's list in the founder's order: Factory · Contractor · Advertising · Real estate · Owner · Consultant · Station management · Workshop · Personal · Other.
- D3 Suppliers N, K, C, D: the full name equals the code until the admin types a real one (FACET recorded none; the founder said the code is the name).
- D4 Fire ratings B1 · A2 · Normal (عادي). Classes A · B · A2G1 · A2G2. Thicknesses seeded as FACET had them, 2–8 mm, with only 4, 5, 6 active and 4 mm preselected.
- D5 Width chips 1.24 · 1.5 · 2.0 · Other; 1.24 preselected. Length is typed (5.8 is the standard sheet). Colour code is required free text.
- D6 m² and money show two decimals with thousands separators and Western digits in both languages; rounding is half-up per line.
- D7 Countries: FACET's nine (Saudi, UAE, Bahrain, Kuwait, Oman, Qatar, Egypt, Jordan, Syria) plus the rest of the ISO list in English and Arabic; the six pinned first. Cities: FACET's 171 Saudi cities, six pinned first. Both searchable in either script.
- D8 Duplicate warning matches a company name (case-insensitive) or a contact's normalized phone across all reps; it names the company and its rep and never blocks saving.
- D9 A company's next follow-up is set by the latest log entry or by the picker at the top of its drawer. A project has its own. The home strip counts both, plus never-contacted companies older than 14 days; clicking it lists them.
- D10 Quotation numbers are Q-1, Q-2 …; a revision is Q-12/2 and copies every line. Revise is offered on issued, accepted and rejected quotations. A rep may cancel his own quotation while it is requested or returned; nobody cancels an issued one.
- D11 The rep's Customer rejected does not mark the project lost; Mark lost stays a separate action on the project.
- D12 Dispatch numbers are D-1, D-2 …. Shipment methods are FACET's CT · TT · Cargo, editable in Lookups. Destination and payment terms are free text, both required. A dispatch cannot exceed the quotation's remaining quantity per item.
- D13 Notifications are stored as a kind plus parameters and rendered in the reader's language.
- D14 Manager "Stuck": requests waiting more than 2 working days · follow-ups overdue more than 3 days · companies never contacted for more than 14 days.
- D15 Coordinator has no companies of her own; her home is the Queue. Admin sees the manager's home plus an Admin menu.
- D16 Language is saved per user (user menu); theme is saved per browser (cookie), dark by default.
- D17 Passwords are at least 8 characters; deactivating a user ends their sessions at once.
- D18 The first contact added to a company is its main contact; any contact can be made main later.
- D19 Export is three CSV files (companies with main contact, quotations with items, dispatches with items), UTF-8 with BOM so Excel opens Arabic correctly.
- D20 Target card pace compares achieved ÷ target with working days elapsed ÷ working days in the month.
- D21 Contact positions: FACET had no list (free text). Kladra seeds a searchable list the rep can also type over: Owner · General manager · Project manager · Procurement · Engineer · Site engineer · Architect · Consultant · Accountant · Other.
- D22 The log's channels are visit · call · WhatsApp · other (FACET's list without email and meeting, per the brief).
- D23 A company list that comes back empty says why: nothing matched the search, nothing matched the filter, or there are no companies yet — and only the last of the three offers Add company. Offering it to a rep whose search simply missed answers a question he did not ask.
- D24 Archiving asks first, for a company, a contact or a project. It is not destructive, but it takes a row off the rep's floor, and a row that vanishes with no warning reads as a bug. An archived company still opens by link and can still be edited; it accepts nothing NEW — no contact, no project, no log entry, no follow-up date — because those would hang off a row that appears on no list. The admin's restore screen is P6.
- D25 Editing a company, a contact or a project opens the same fields as adding one, on what is already there, minus the thing that cannot move: a company's first contact (contacts have their own list), a contact's company (a person who moves is a new contact there, S11), and a project's company (moving one would take its whole log off the customer it happened at, S18).
- D26 Lists are not virtualized. Fourteen people, and a rep sees only his own companies; the day a manager's list passes a few hundred rows is the day to revisit it.
- D27 An unused message key is a lie about what the app says, so `npm run check:messages` now fails on one. A key built at runtime marks its whole prefix reachable — the alternative is deleting a string a screen shows.
- D28 The tests own a second database, `kladra_test`, and a second port, 3101 (`npm run dev:test`). They delete every row and seed fresh ones on every run, so sharing the development database meant a test run wiping the records somebody was looking at — which happened, and half of one review's findings were symptoms of it. The name is derived by appending `_test` rather than configured, so no `.env` can point a run at real data by omission; before deleting anything the suite asks `/api/health` which database is behind the port and stops if the answer is wrong. DEFAULT — founder may change.
- D29 Development servers listen on 127.0.0.1 only, like the deployed ports. `next dev` and `next start` default to every interface, which puts a signed-in copy of the CRM with the seed accounts on the office Wi-Fi. DEFAULT — founder may change.
- D30a A screen is marked live once React has taken over its HTML, and the tests wait for that mark after every page load. Without it a press that lands in the gap does nothing and the run fails at random. DEFAULT — founder may change.
- D31 **Overrules a §3 line** — "Every empty list shows one sentence and its primary action. (all)". An empty list shows one sentence and its primary action *where that action exists*. What was observed: every screen whose slice is not built yet rendered the action its sentence named, disabled, wearing the brand gradient — the one signal reserved for "this is the thing to press". On the empty companies page both Add company buttons came up that way. Pressed, nothing happened, and the screen read as broken rather than as unfinished, which is the exact impression that killed FACET. So where the slice is not built the sentence carries the news alone and, where the work can already be done somewhere else, says where; the action comes back with its slice. Everywhere the action does exist the §3 line stands unchanged, and D23 covers the third case, a list emptied by a search rather than by having nothing in it. DEFAULT — founder may change.
- D30 Arabic UI text addresses nobody's gender: the verbal noun rather than the imperative, and "الرجاء" where an instruction is unavoidable. Rawan reads this app all day and half of it was written to a man. `npm run check:messages` fails on the marked forms. DEFAULT — founder may change.
- D32 A rep may **withdraw** his own quotation request while it is still waiting or has been sent back, and only then. It leaves the coordinator's queue and keeps its whole history; nothing is deleted (S16). §3 gave the rep a way to raise a request and the coordinator a way to answer it, and no way to take back a request raised on the wrong project — which in FACET meant Rawan pricing something nobody wanted and finding out on the phone. The word is **Withdraw**, never "cancel": a cancelled quotation sounds like a lost job, and D11 already says a rejected quotation is not a lost project. DEFAULT — founder may change.
- D33 Every date on a screen is rendered by one component, and it takes the direction of its own text — never a forced `ltr`. Thirteen call sites had each decided that separately, so `04/سبتمبر/2026` could sit twice in one drawer under two conventions. Arabic reads it day-first from the right, which is how an Arabic date is written; English day-first from the left. A Playwright test measures the rendered order rather than reading the DOM, because the text is identical either way round. DEFAULT — founder may change.
- D34 The number a screen shows and the number WhatsApp is given are both the stored E.164 one, never the one the rep typed. The typed form exists only to fill the edit form back in. It is a distinct TypeScript type, so `formatPhone` and the wa.me link accept nothing else and `npm run typecheck` catches the substitution — which had already shipped: one contact's number showed ungrouped in the company drawer and its WhatsApp link carried no country code. DEFAULT — founder may change.
- D35 Anything that opens a dialog is rendered in one position, whatever the list under it happens to say. A trigger rendered inside an empty state is destroyed by the very save that fills the list, and a destroyed component's success effect never runs: the first quotation a rep raised on a project saved in silence and left him on the project, while the second one worked. Where the answer has to survive the form's own disappearance — raising a revision removes the button it was raised from — the action is awaited and answered on directly rather than through an effect (`useSubmitAction`). Where the drawer's action row already carries the button, the empty state carries its sentence alone rather than a second copy of it. DEFAULT — founder may change.
- D36 A dispatch goes against the LIVE revision of a quotation, never a superseded one. §3 and S38 say "an issued quotation"; once it has been revised the customer holds the new paper, and goods sent against the old one move on a price nobody agreed (S34, S35). The button is absent on a superseded revision and the action refuses it in a sentence. DEFAULT — founder may change.
- D37 A dispatch has no withdraw, and that is deliberate rather than an omission — the quotation chain has one (D32) and this one does not. A dispatch request the rep raised by mistake is refused by the coordinator with a reason, which is the conversation that was going to happen anyway and leaves the mistake and its answer both on the record. S41 already says a dispatch that goes wrong is replaced by a new one rather than edited, so a second way to make one disappear would argue with it. DEFAULT — founder may change.
- D38 Every figure the browser adds up while somebody types is computed by the same function the database uses, and a test compares the two on real numbers. `1.24 × 5.8` is `7.192`; rounding that to `7.19` before multiplying by thirty gives 215.70 where the answer is 215.76. The gap is six halalas and nobody would ever report it — they would just stop trusting the screen. DEFAULT — founder may change.
- D39 Countries and cities are not in Lookups. They are ISO reference data — 249 countries and 171 Saudi cities, seeded from a standard — and which few sit pinned at the top of the picker is a design decision rather than an opinion about the world. Renaming Saudi Arabia is not something an admin needs at ten at night; adding a company category is, and that is what the screen is for. DEFAULT — founder may change.
- D40 A lookup row is edited through the boxes its own list has, decided in code and never by the form: a supplier carries a code and a full name, a shipment method a code and both languages, a thickness a number in millimetres. One shared "name" box would have written an empty code into two tables that refuse one. The row reads back on one line as its values joined by · — "Factory · مصنع" — and that joined line is the name every confirm dialog says back. DEFAULT — founder may change.
- D41 A target box left blank clears the target rather than storing a zero, and each box saves on its own. Zero says "aim for nothing", which is a different sentence from "no target set" — the team screen shows a dash for one and a figure for the other, and every other figure on the row stays real either way (S45). One Save for a screen of eight boxes is how an admin who fixed one number and walked away changes nothing. DEFAULT — founder may change.
- D42 A manager and an admin read every rep's floor and write on none of it. S8 says the manager SEES everyone's companies, and WORKFLOW §3 says his screen is read-only; the code asked one question — "may he touch this?" — got "yes, he is the manager", and let him edit, log against, archive and re-follow-up any rep's records for three phases, with the drawer offering the buttons. Seeing and changing are two questions now (`mayOpen`, `mayWrite`), every write asks the second, and no screen offers work the action behind it would refuse. A manager who sells is not an exception: his own id is on his own companies, so he writes on them for the same reason Faisal does. The history of a company is the manager's report (S27), and a report he partly wrote is not one. DEFAULT — founder may change.
- D43 A refused box is answered at the box, on every form, not only on the ones a rep uses. `useSubmitAction` kept the action's whole-form sentence and threw its per-field answers away, so five dialogs built on it — add a user, a lookup row, a holiday, a dispatch request — put "Required" at the bottom of the dialog and marked nothing. An admin filling a category in two languages could not see which language was missing. The hook hands back the whole refusal now, the footer carries the sentence only when no box is carrying one, and the caret moves to the first refused box because a dialog can be taller than a phone. DEFAULT — founder may change.
- D44 Who carries metres is one sentence, used by both screens that ask: reps and the manager. The team table listed everybody who was not the coordinator, so the admin sat on the manager's main screen as a permanent row of dashes — no target, no companies, nothing achieved — while the targets screen already refused to give him a box. Two screens disagreeing about who has a month is how a figure ends up with two definitions. DEFAULT — founder may change.
- D45 A drawer takes focus itself when it opens; a form dialog still focuses its first field. Radix focuses the first tabbable control, and in the company drawer that was the follow-up date picker — so following a shared `?open=` link put a focus ring on a date nobody had touched and armed Enter to open a calendar. A drawer is a place a rep arrived at, not a question he was asked. Nothing is drawn round the panel's own edge, because an outline on something a person cannot act on says nothing; every control inside it keeps its ring. DEFAULT — founder may change.
- D46 Every value a sentence is handed is isolated from the sentence. The app wraps a code in `dir="ltr"` wherever a screen renders one itself, but a translated sentence cannot reach inside its own placeholders: `طلب {rep} عرض السعر {label}.` comes back as one flat string, and the full stop, the colon and the «guillemets» around a search term then settle against the paragraph instead of against the value they belong to. A customer called "3M Arabia", a project name in English inside Arabic quotes, an emailed reason — each puts a neutral character beside a run whose direction only the run knows. Unicode isolates fix it in one character each side, and they are added once where every message is loaded (`src/i18n/isolate.ts`), not by hand at forty call sites: a message written next year is safe without anybody remembering. Plurals keep their shape — `one {day}` is a branch, not a value. DEFAULT — founder may change.
- D47 On the manager's and the admin's screens, "Company" means Technopanel, and everywhere else it means the buyer. S44 is the founder's own sentence — "the company target is one figure per month" — so the label keeps his word rather than being quietly renamed, but the card does sit above a table of customer companies and the collision is real. It is one string each side (`team.companyMonth`, `admin.companyTarget`) if Jerom would rather read "Technopanel this month". The unit under it was wrong and is fixed either way: the target is set in square metres, and the hint said metres. DEFAULT — founder may change.
- D48 The month bar carries two marks and takes its colour from the gap between them. S46 forbids combining target and activity into one score, and nothing here does: the figures still sit side by side and the bar still shows the two of them, the fill for what has gone out and a notch for how far through the month the calendar is. What P8 added is the colour Jerom asked for on that gap — ahead is green, a little behind amber, properly behind red — and the band is deliberately wide, because a rep four per cent short on the eighth of the month has not gone wrong and a screen that says he has stops being read. In the first five working days it is grey, for the same reason the sentence beside it says the month has just started (S49). The bar was painted with the brand gradient before this; a gradient on a measurement is decoration, and DESIGN §4 keeps it for the primary button. DEFAULT — founder may change.
- D49 A rep's home is his day, not his company list. D15 gave each role one home and gave the rep his companies, which answers "who are my customers" — a question he asks a few times a week. The question he opens the app with is "what do I do now", and until P8 nothing answered it: he had to read a list, remember the dates and decide. `/day` answers it in one column, in the order the work should be done — his month, what has come back to him and is stopped, then who is owed a call, in three bands worst first. The company list is one press away and is still where he searches. The coordinator and the manager keep their own homes, because their screens already ask their own daily question. DEFAULT — founder may change.

- D50 Marketing is a role, and what it cannot do is put a price in front of a customer. §1 said "marketing works as a rep for now" and P8's seventh item asked whether the four roles are enough; this is the answer. Marketing owns companies, logs, sets follow-ups, opens projects and reads its own day exactly as a rep does, and stops at the quotation — the price is the sales conversation, and the person who has it is the rep the lead is handed to. It carries no monthly target for the same reason: metres are what a sale moved, and a role that never closes one would read as a permanent shortfall (D44). Three sentences carry the whole thing — `ownsCompanies`, `sells`, `holdsFloor` in src/lib/floor.ts — and the guards that let an action run are the same lists, so a screen and the server cannot answer differently. The seed carries the account, not a person: nobody has been named to it. What is deliberately NOT built is a figure for what marketing produced — leads found, leads handed on, leads that became metres. Handing a company over gives it away, so after the handover marketing's own screens are silent about it, and that is the honest state until Jerom says how the role is measured. How you measure somebody changes what they do, and it is his decision, not a default worth guessing. The audit log has every handover with both names in the meantime.
- D51 A company can be handed to somebody else, and only its owner, the manager or the admin may do it. Without it marketing is a dead end — it finds a customer it may never quote and has no way to pass him on — and a rep who leaves takes his floor out of sight with him, because every list is scoped by `companies.rep_id`. That one column is also why the move is one statement: projects, quotations, dispatches and achieved metres are all read through it, so they travel with the customer, which is what S8 already said about whose metres these are. The log does not travel — an activity records who did the thing, and rewriting that would be rewriting the report (S27). A manager doing this is not a hole in D42: he still writes nothing ON a floor, and who a customer belongs to is a different question from what happened with him. Both names go in the audit row and the new owner is told. DEFAULT — founder may change.

- D52 What a row may contain is the database's rule, not only the form's. The schema had almost none of it: a quotation line could carry a zero quantity or a negative width, the same line could sit twice on one dispatch and double the metres the month is measured by, a company could have two main contacts or no city at all, a revision pointed at a bare uuid with no foreign key, and an issued quotation could carry no issued_at — which is not an error anybody would ever see, because it reads as a request that has waited zero days for ever on the screen that says what is stuck. Zod says all of this at the door, and the door is not the only way in: the seed writes rows, migrations write rows, and an import will write rows next year. Fifteen checks, four unique indexes and one foreign key now say it where it cannot be bypassed (migration 0002), and every one of them was proved to refuse a bad row before it was believed. This is the cheap moment: there is no production data.
- D53 The same SMAC number cannot be typed twice. S4 says every SMAC number is typed by a person and can be wrong, and the app took that as licence to accept anything: two quotations could carry one number, which is the only link between Kladra and the system that holds the money. It can still be wrong. It can no longer be the same wrong twice.
- D54 There is no events table; the audit log is the history. "How many times did this come back?" and "who typed the customer's answer?" need a record of every transition, and one already exists — every action writes an audit row with who, what and when. A second history table beside it would be a second answer to one question (rules/data.md), so what was added instead is one index, on (action, at), because without it the question is a full scan of the one table that only ever grows. Every transition already writes its row, the customer's decision included. The row keeps the instants — when it was issued, when it was decided — because those are the current state and every screen reads them; the audit answers what happened in order. DEFAULT — founder may change.

- D55 The system writes the day; the person writes the sentence. **This overrules S27**, and only its second half. S27 says a company's history IS the manager's report and there is nothing to write or submit, and the first half of that is right and is the whole design of the report screen: not one figure on it is typed by anybody, because visits, quotations, dispatches, metres and calls due are all read back out of records the work itself produced. What S27 got wrong is that a history says what HAPPENED and never what it MEANT — "Delta Rock have not called me back for the third time, I think they went to the other supplier" is derivable from no row in the database, and it is the sentence a manager reads a report for. Jerom's own observation is the evidence: the Google Sheet is still open, and the one column of it Kladra had not replaced is that line. So there is a report, it is one box of free text, and it is never a form of dropdowns — every tool that asks a rep to retype the half the machine already knows is a tool reps abandon, which is what four channels of activity are already the maximum of. Under a minute, on a phone, at six in the evening. DEFAULT — founder may change.
- D56 The report is the team's, not the manager's. The three roles that face the work write one — rep, marketing, coordinator — and the manager and the admin read and do not file: that is exactly what the habit being replaced was, and a manager writing a report to himself is a ritual. One screen: your own day at the top with the box in it, everybody else's underneath, alphabetically, and the manager reads exactly the same page. A report addressed to one person is a report to the boss and gets written like one, and the sheet this replaces was one the whole floor could open. The anti-leaderboard rule that follows is not a preference — nothing on this screen carries a target, a pace or a percentage, so there is no ranking to read into the order, and S46's ban on one number that mixes target with activity cannot be got round by putting the ingredients on a different screen. It is also why the participation line is a count and not a list of names: "4 of 5 wrote" beside five cards is arithmetic anybody can do, and saying the names would make it a roll call. **What is stuck is not repeated here.** Jerom asked for who moved, who did not and what is stuck in one scroll; the first two are the cards, and the third already has one definition and one home — the stuck list on the manager's own screen (D14, S45) — and a second copy of it beside the reports would be the drift trap rules/data.md exists to stop. What this screen answers about stuck is the half the team screen cannot: the sentence a person wrote about it, and the red line saying a call fell due and was not made. The report is where a manager reads the DAY; the team screen is where he reads the MONTH. DEFAULT — founder may change.
- D57 A day that is not over cannot be missed, and being away is not being silent. Four states, and only one of them means a report is actually missing: written, open (the day has not finished), off (a Friday, a Saturday, a company holiday, or that person's own leave) and silent. The weekend and the holiday table are the same `isWorkingDay` the pace line and the stuck list already use, asked per person, so a rep on leave shows "On leave" and is subtracted from the number who owed one. A missed day is one dashed, empty box where the sentence would be — no badge, no icon, no colour anywhere on the card, and the figures stay: the system saw the work and says so, and what is missing is only the half he alone could write. Jerom asked for it to be visible without being a punishment, and an outlined blank is a form with a gap in it rather than a mark against a person. DEFAULT — founder may change.
- D58 A report can be written for today and for the last WORKING day before today, and then it closes. The rule was "today and yesterday" for one afternoon, and it was wrong in Riyadh: the week ends on Thursday, so on a Saturday the last working day is two calendar days back and not one person on the floor could write anything at all — the screen shipped, seeded and looked complete, with no box on it. A rep who closes at six on Thursday and remembers something on Sunday morning is the exact case a grace day exists for. The window still closes, because a report rewritten a week later is a reconstruction and not a record. It is checked in the action and not only on the screen, and the day the screen opens on is by construction always inside the window. One more thing the column enforces rather than the form: a report has to contain a character that is not whitespace, and the first version of that check used `btrim` with no second argument, which trims spaces and not tabs or newlines — so it accepted a report of newlines while Zod refused one. A column looser than the form it stands behind is worth nothing (migration 0004). DEFAULT — founder may change.

- D59 A figure carries the words that say what it means, and a length is not a date. Jerom's phase-9C rule is one sentence — every number must answer a question somebody asks daily, and the screen must say what the number means in words beside it — and it is a slot rather than a habit: `StandingStrip` takes a CAPTION under every value, and a caption is never a definition of the figure. "The number of quotations" is a tooltip and nobody reads it twice; the caption is the reading — what the figure is measured against, or which part of it somebody has to act on. Two of the coordinator's four changed shape to earn one. "Waiting since 03/Sep" was the right fact asked the wrong way round: a date makes the reader do the arithmetic, and in a Fri–Sat week the arithmetic they do in their head is wrong by two, so it is a length now, in working days, with the date as the caption. And "answered today" had nothing to be measured against, so what arrived today sits beside it — five answered against four arrived is a day going well and five against twelve is not, and it only goes green when she is level. Each waiting row says its own age, and past two working days it is red: the manager's screen has called a request stuck at that line since P8 (S53), and until now the one person who could clear it was the only one not told. One rule, one file (`src/lib/waiting.ts`), asked by both screens. DEFAULT — founder may change.

- D60 One name is one number, and a figure with a threshold in it says the threshold. An inventory of every figure on every screen — the second half of 9C, and the reason for it is that "delete any figure not attachable to a question" cannot be done without first listing them — turned up one genuine collision and several near ones. The genuine one was on the manager's own screen, twice over: the strip counted follow-ups more than three days past their date under "Follow-ups overdue", and the table under it counted every overdue one under "Overdue follow-ups". Two different numbers, side by side, their names a letter apart in the reading. Both are worth having; what was missing was any way to tell them apart, so the strip's is "Follow-ups long overdue" now and its caption says "more than 3 days past its date". The same caption went on the other three: what the pipeline counts, how long a request has sat, how old an uncontacted company is. In English only, "Quoted" meant two different units — sheets on a dispatch line, square metres on a project — while the Arabic had told them apart all along (في عرض السعر against المعروض); the dispatch line says "On the quotation" now, which is what the Arabic says. A manager drilling into a rep's floor got the rep's bare NAME over Achieved, Target and Pace, three figures whose whole meaning is the month they are in, so that card says whose month and when. And on a phone the team card showed "1,200 / 1,500 m²" with no word for either side, which is the rule broken outright: the label is the sentence the slash was standing in for. What was NOT changed: a bare m² on a board card or a mobile row, where the unit is present and the subject is the row it sits on. Jerom's rule is about numbers that need interpreting, not about a word beside every digit. DEFAULT — founder may change.

- D61 There is a month before this one. Kladra had none, anywhere: every screen showed the current month and nothing else, which is item 11 on the phase-9A list and one of the five questions Jerom named. "1,180 m² against 1,500" is a fact with nothing to be measured against, and the question everybody actually asks in the second week is not how am I doing but whether it is better or worse than last month — which the Google Sheet could answer, because a sheet has rows above the one you are on. Six bars, on the manager's screen for the company and on a rep's own day for himself. It is still the only chart this app has (CLAUDE.md): no line, no smoothing, no average, because fourteen people on cladding cycles have lumpy months and one tower approved on the 28th is half a target, so a trend line would draw a shape the business does not have. Every month's figure is written above its own bar, so the bars carry only the shape and are hidden from readers — there is nothing in the picture that is not in the text. A finished month is coloured against its own target using the same three bands as the month card (D48); the current one is grey, because painting a fifth of a target red on the third of the month is a screen people stop opening. One sentence beside it compares the last FINISHED month with the one before, never with this one. And the seed gained five months of business that actually happened — twelve accepted quotations each with an approved dispatch, dated inside their own months — because a card whose every bar is zero is a card nobody has seen work (rules/data.md), and because a history made of the same records as the present is read by the same query as the present rather than by a second definition of achieved. DEFAULT — founder may change.

- D62 Where quotations go is asked of a COHORT, not of the board. Another of Jerom's five, and item 5 on the phase-9A list: the app could say what every quotation IS and nothing about what becomes of them. A funnel of current statuses would have been the board with its columns stacked, and the board is one screen away — so this takes every quotation raised in the last ninety days and follows it forward to the furthest point it reached, counted once, at that point. Six endings: still on the desk, sent back and never asked again, withdrawn, out with the customer, accepted, rejected. They sum to what was raised, which is the check a spec can hold. A revision is its own row, because raised twice and issued twice is two trips through the chain and pretending otherwise hides the rework this exists to find (S32). One line under it carries the only conversion in the chain that is about the market rather than about us — of the ones that actually reached a customer, how many he answered; everything above it is our own handling. It lives on the manager's screen and not on a rep's day: his day is a list of things to do before lunch, and this is a number to think about. If Jerom wants a rep to see his own, the query already takes a floor. The seed gained eight quotations that did not land — a rejection, a withdrawal, one sent back and abandoned, two still out — because twelve accepted history quotations made the cohort read 43% won against a first draft of 63%, and a funnel with one fat stage teaches nobody anything. None of them carries a dispatch, so no month moved. DEFAULT — founder may change.

- D63 A customer nobody has a next step for is on a band, and it is the fourth one. The five-day walk found the leak and phase 9C had to answer it: every band this app draws is keyed on a DATE — overdue, due today, added and never contacted — and a customer who was contacted once and then dropped has no date on him at all, so he was on no band of any screen. Eight of Faisal's twelve were in exactly that state. Gone quiet is the other half of never-contacted and the bigger half: a company nobody ever called is a lead that went nowhere, and this is a conversation somebody started and stopped. The rule is the one every screen shares — no effective follow-up anywhere on the company or its live projects, at least one activity ever, and nothing logged for fourteen days. Fourteen because it is the same threshold S51 already set for never-contacted, and a second number would be a second rule to explain; the two questions are the same question asked of a customer before and after his first call. It is on a rep's day, last of the four bands, because nobody is expecting a call from these today — and it is on the manager's stuck list beside the other three things that have stopped, with the rep's name on each row. It is a pill on the customer list too, so the band and the list open the same set from one definition (rules/data.md). Both places print the rule in words under the heading, because "gone quiet" is a threshold pretending to be an adjective (D59). Four companies in the seed are in this state, across three reps. The Arabic settled the naming and the English followed it: انقطع التواصل, which is the verb Arabic pairs with تواصل and makes a matched pair with دون تواصل for never-contacted — one saying contact never started, the other that it broke off. Not صامتة, which says the customer chose the silence; what the query measures is that nothing has been LOGGED, which is our side of it. The rule under both headings is one key in `common` now rather than two sentences in two namespaces that had already drifted apart in English. And the two pills that count COMPANIES say so, because they sat on a strip labelled Follow-ups beside two that count follow-ups. DEFAULT — founder may change.

- D64 A union and a list of its members are one thing, and the second copy is the one that drifts. `FollowUpFilter` gained `"quiet"`; `FILTERS`, the runtime list the URL parser tests against, did not — so `?filter=quiet` parsed to undefined, the pill dropped its filter, and the customer list opened WHOLE while its heading said these were the quiet ones. A screen lying about what it is showing, from one word missing in a second list, and typecheck could not see it because both copies were legal. The rule was already written for messages — "never write the members out beside the union" (rules/words.md) — and it was written there because the same shape had already cost a phase. So it is now the shape everywhere: the list is the source, `type X = (typeof LIST)[number]` is derived from it, and adding a member to the list is the only way to add one. The sweep found the role vocabulary in three copies — the union in `types.ts`, the pgEnum in `schema.ts`, the picker in the admin panel — none of them drifted yet, all of them able to. One list in `types.ts` now, imported relatively by the schema so drizzle-kit can read it without the alias, and the words check reads that list instead of the union it used to. DEFAULT — founder may change.

- D65 A label is never truncated; a label that has to be cut is carrying something it does not need. The six-month card wrote "Sep 2026" into a column 47 pixels wide on a phone and let CSS cut it. In English it read "Sep 20…", which is ugly and harmless. In Arabic the same cut fell the other way and «سبتمبر 2026» came out «سبتمبر 6…» — a year that reads as a different year, on a chart axis, where the whole job of the label is to say which month this bar is. The fix is not a smaller font or a wider column, both of which only move the width at which it breaks: the year is the same on five of the six columns, so the month name goes on one line and the year on a second, inked on the first column and wherever the year turns over, with the slot kept empty on the others so the six bars keep one baseline. This is the general rule for any axis, and it is the reason nothing in this app is measured by reading English pixels: a truncation that is tidy in one script can be a wrong number in the other. DEFAULT — founder may change.

- D66 A colour the demo never shows is a colour nobody has seen work. The rule was written for figures that are always zero (rules/data.md) and it is the same defect one step along: the company's monthly target in the seed was 4,500 m² against months that ran 1,942 to 3,524, so every finished bar on the manager's six-month card was the identical red. The three pace bands existed in the code, were tested, and could not be seen — and the first thing the manager's screen said about the last five months was that all of them failed, which is not what the seeded business did. The target is 2,600 for finished months and 3,200 for this one, which is near what the floor actually approves, and the six bars now carry red, amber and green. It is not a coincidence that both halves of this rule were found by looking at screenshots rather than by reading code. The same pass found the third form of it: a band that is exercised SOMEWHERE is not the same as a band anybody has seen. Faisal is the rep every demonstration is given on, and he had no company that was added and never contacted — Marketing had one and Saad had one, so the band was in the seed, green in the tests, and absent from the screen Jerom opens. He has one now, and his day shows all four bands at once. DEFAULT — founder may change.

- D67 The first screen was the last one built to the app's own rules. Phase 9E starts with sign-in because it is the only screen a signed-out person can reach and therefore the first thing all fourteen of them see, and it had drifted three ways at once — none of them visible to anybody who only read the screenshots, and all three found in an afternoon by reading the file next to the ones it should have matched. The Kladra mark was drawn a second time here, in `bg-brand`, a token that is a different colour in each theme, against DESIGN's own rule that the mark gradient never changes because a logo does not change colour when somebody turns the lights off. The card was the component library's, the only one left in the app: a ring where every other surface in Kladra has a border, no shadow, and none of the hairline highlight that makes a card look lit. And the page had no heading of any level, because a CardTitle renders a div — so the one public page in the system gave a screen reader nothing to land on. The pixels then found the fourth, which reading could not have: the card grows by one line when a sign-in is refused, the whole block is centred on the canvas, so the wordmark rose eighteen pixels and the language link fell eighteen at the exact moment somebody was reading why they had been turned away. The error line is in the layout from the start now, empty until there is something to say. The password field also gets `dir="ltr"`, like the address above it — it had inherited the page's direction and started its dots from the right in Arabic. What that costs is 56 pixels of empty space above the sign-in button on a screen where nothing has gone wrong, against 16 between the two fields, and it is worth it: the alternative anchors the block to the top of the canvas, which stops the wordmark moving but still slides the language link down while somebody is reading. Nothing moving at all is the only version a person cannot notice. DEFAULT — founder may change.

- D68 A person is named in the reader's script. Every lookup in this system already carries two names — a city, a category, a lead source, a thickness, a position — and the people carried one, so an Arabic screen named Rawan's colleagues in Latin all day: "Faisal Al-Harbi" under a heading reading المندوب, and a role account whose name was the English word Marketing. The Arabic reviewer found it while checking the 9C strings and it is not a translation question, which is why it waited for this box: it is a schema question about whether a person has one name or two. Two, with the second optional — `users.name_ar`, null or empty falling back to the Latin one — because a name typed twice can disagree and the second copy is the one that drifts (D64), and an account added in a hurry has one name and should show it to everybody rather than show a blank. One helper resolves it, `src/lib/people.ts`, and thirty-odd queries were swept onto it; the sweep was possible without touching a single caller because every one of those functions is already async and already inside a request, so it can ask for the page's locale itself. Three places keep the Latin name deliberately and say why in a comment: the CSV export, which takes the English column for the country and the category beside it because a CSV is a machine's file; the audit log, because a record of what happened should read the same to whoever opens it later; and the admin's own user list, which reads both raw because its form edits the pair — its TABLE resolves like every other screen, which a spec found by timing out on a click against a Latin name that was no longer there. `npm run lint` now fails on a query that names a person in Latin on a screen — it found three the hand sweep had missed, in page files nobody thought to grep. The seed gives six of the seven an Arabic name and deliberately leaves Turki without, so the fallback is a thing somebody has seen rather than a branch nobody exercises. DEFAULT — founder may change: the alternative is one name for everybody, which is what most systems in Riyadh do and what FACET did.

- D69 The identity is written once, or it is not an identity. The rest of 9E was an audit rather than a design: every card surface, every gradient, every brand-coloured background and every number on seven screens, in both themes and both languages, measured from the computed styles rather than looked at. The good news first — `card-face` is applied correctly in all sixty-one places it appears, the two page backgrounds are the designed warm off-black and warm off-white rather than pure black and white, money and m² are in the number face everywhere, and the frosted surfaces are exactly the four that are meant to be frosted. What the audit found was drift in the places nobody looks at twice. The primary button, the one control DESIGN §2 says every screen has exactly one of, was a class string written by hand in fourteen files, in two syntaxes and under two token names for the same colour; it is `variant="brand"` now, in fifteen places, and `npm run lint` fails on the fifteenth copy. Every floating surface the component library ships — dialog, drawer, menu, popover, select — drew a RING in its own colour, which sits outside the box and matched no border in the app; they take the `--line` border now and keep their own tighter shadows, because a menu that appears for two seconds should not wear the shadow of a card that has been sitting there all day. The loading placeholders were 12px where the cards they stand in for are 14px, so a list changed shape as it filled. The keyboard hint in the search bar wore the `.num` class, which means "this is a figure" and also sets tabular figures — meaningless for the letters Ctrl K. And a third mark gradient, for a contact's initials circle, had been in the identity table for eleven months and was used by nothing: an identity table listing a colour nobody can see is a table that cannot be checked, so it is gone. Two things the audit raised were decided rather than fixed, and are written into DESIGN so the next audit does not raise them again: the inset strip has no shadow on purpose, because a shadow says lifted off the page and it is not; and a date is not set in the number face, because in Arabic it is `03/سبتمبر/2026` and the mono face would render the month name in a fallback, making the one string on the screen that mixes two scripts also mix two typefaces. DEFAULT — founder may change.

- D70 A log entry can be corrected, and unfiled. Item 3 on the 9A list, and the highest one still open: there was exactly one action on the log and it was `log`. A visit typed against the wrong customer was wrong for ever, and the only correction available — a second entry saying so — is one that every count afterwards believes: two logged calls where one happened, two companies touched where one was, and a "last activity" on a customer nobody spoke to. Two things now, with different windows because they answer different mistakes. **Correcting** changes what he meant to write — the words, the channel, whose meeting it was, which project — and is open while the entry's DAY is, which is today or the last working day, the same window the daily report already uses (D58); a record that can be rewritten a week later is not a record. It does not touch the day, which is the entry's identity, and it does not touch the follow-up, which is a figure the company row and two bands on the day screen read. **Unfiling** is for the wrong customer: the entry leaves every list and every count, the row stays (S16), and the whole text goes into the audit line, because what is being taken off the screen is the only copy of it. There is no restore — a person who unfiles the wrong entry writes the right one, which is what he was doing anyway. Both are for the AUTHOR only. A manager reads every rep's log and may not rewrite it: testimony belongs to whoever gave it, and "he changed what I wrote" is the fastest way to lose a floor's trust in a system. Sixteen queries count the log and every one of them now excludes an unfiled entry — the sweep that mattered, and the reason the test counts rows rather than looking at the screen. The seed carries one unfiled entry so the exclusion is a thing somebody has seen work. DEFAULT — founder may change.

- D71 The rep says what happened without leaving his day. Item 4 on the 9A list, and the cheapest minutes on it: the day screen lists who to call, with the phone number on the row, and then sent him somewhere else to write one sentence about the call he had just finished — press the row, wait for the customer list to load, press Log, type. Two of those five steps were navigation, standing in a lobby, which is exactly the tax that made FACET's reps stop logging and the sheet the record instead (S23). The log dialog opens on the row now, with the same two lists it has in the drawer: the company's contacts and its live projects, fetched for the whole screen in two queries after the bands are known rather than two per row. The button sits above the stretched row link with the phone number, the only two things on that card that are not the card. What this does NOT do is shorten the dialog: the walk said logging must take under a minute, not that it should ask less — the channel still defaults, the day still defaults to Riyadh's today, and the follow-up is still one tap away, because the follow-up is the thing that puts him back on this screen tomorrow. DEFAULT — founder may change.

- D72 A quotation remembers what happened to it, and a reason dies with the state it explains. Item 5 on the phase-9A list: the chain recorded where a quotation IS and threw away how it got there, so one sent back twice and one sent back five times read identically on every screen — the row carries the reason of the LAST return and no memory of the one before, and the count of returns is the figure the manager asked for by name. The trail is read from `audit_log` rather than from a history table of its own, because every transition already writes a row there inside the same transaction as the change, and a second table beside it would be a second answer to one question (rules/data.md, and the note on the table in the schema). The eight things that can happen are one list in one file: the actions build their audit string from it, the trail types itself from it, and `npm run check:messages` demands a sentence in both languages for every member — an action with no word for it is dropped before it reaches the screen rather than arriving there as a missing-message crash. The second half of this came out of the same walk. The reason outlived the state: the rep fixed what she had sent back, the status went to `requested`, and her words stayed on the row saying something untrue about a corrected quotation — invisible, because every screen asks the status before it prints the reason, and therefore wrong in the one way nothing ever catches. The dispatch beside it has carried `(refuse_reason is not null) = (status = 'refused')` since the schema was written and the quotation carried nothing; two columns doing the same job, one of them guarded, which is the whole story of how it happened. It is a constraint now, both ways round — a returned quotation has a reason, and nothing else does — so an action that forgets fails at the write instead of quietly in the column, and withdrawing a request clears it as well. The seed writes the trail from the facts it already holds, for the current quotations and for the five months of history behind them, including one request she sent back twice: a panel that is empty in every demo is a panel nobody has ever seen work. One thing was decided rather than fixed. The trail's words repeat the status words — "Sent back" is where it is now and also the thing that happened on the 27th — and that is exactly how a person reads a history beside a state, so the words stay and the specs stopped matching bare text: a spec that means the badge asks for the badge, by the tone attribute every state badge already carries. DEFAULT — founder may change.

- D73 A revision is asked for, not issued, and four strings had it the wrong way round. Writing one word for the moment a rep raises a revision — the trail needs a line for it — turned up what the app had been saying since P4: the button read «إصدار مراجعة», to issue a revision; the toast said one had been created; and two error sentences told him a change to an issued quotation "is made by issuing a revision". None of that is what happens. A revision is a new row taking the default status, `requested`, which lands in the coordinator's queue and waits for her to put SMAC's number on it — the same trip the first ask makes, and nothing has gone to the customer until she issues it. So the rep asks: «طلب مراجعة» on the button, «تم إرسال طلب المراجعة» when it is sent, «طُلبت مراجعة» in the trail, parallel to «طُلب عرض السعر» above it, and the two errors say the change is made by asking for one. The English was already right at the toast ("Revision raised") and wrong in the trail, which said "Revised" — a state, where the line beside it names a person and a day. The same defect one line up: "Lines changed" reads as though they changed by themselves, and somebody changed them; it is "Lines edited" now, which is also the word on the button he pressed. This is the second time a word has been settled by writing the sentence that has to sit next to a name and a date — a screen that says who did it will not accept a verb with nobody in it. DEFAULT — founder may change.

- D74 The last one is offered: the sheet above, and the quotation before. Item 7 on the phase-9A list, and the plainest sentence in the five days — nine fields a line, four of them dropdowns with no default, a floor that sells the same specification to the same customers over and over, and nothing offering him the last one. Two offers now do, each of them one press. **Add item** opens the new card on the sheet above it: supplier, fire rating, class, thickness, width and length, because every line of one quotation is usually that same sheet in a different colour. What the SHEET is carries over; what the LINE asks for does not, so the colour code and the price are still typed — a number a screen writes into a form is a number nobody checks, and the price is the figure the whole quotation is measured by. **A first ask at a customer who has been quoted before** carries "Copy the items from Q-12" above the lines, which fills the form with that quotation's items as they were typed, prices included, for him to change. Any status, deliberately: one the customer rejected on price describes the same panel, and what is copied is the specification rather than the outcome. It is offered only while there is nothing to lose — one item, nothing typed into it — and it leaves the moment he starts, because a control that throws away somebody's work either asks first or is not there. Neither offer appears on Edit or Revise, which already open on lines of their own. The offer follows the company rather than being fetched once, because on the Quotations screen the company is not known until he picks the project; the answer carries the company it is about, so a changed pick cannot leave the last customer's quotation on screen under a new one's name. The nine field names had been written out three times — the drawer's draft for Edit, the same for Revise, and now this — and are one mapping now (`src/lib/quotation-draft.ts`), which is the same rule as D64 applied to a shape instead of a list. DEFAULT — founder may change.

- D75 Leave is on the screen where work is handed out. Item 9 on the phase-9A list, and four words in the five days: nobody covers a floor. Leave has been in this system since the schema — a row in `non_working_days` with a person's id on it, entered by the admin — and it reached exactly two things: the pace arithmetic, so a rep back from two weeks off does not read as behind (S48), and the daily report, which marks his day off rather than missing. Everywhere else his floor simply stopped. His customers came due, the reminders were his and nobody else's, and the manager's screen said nothing at all about why a rep with eleven overdue follow-ups had called nobody. Three places say it now. His row on the team screen carries the day he is back, under his name rather than in a column of its own, because it is true of the person and every other cell on that row is a figure. The manager's "what is stuck" list gains a FIRST group — what is due today or already past on the floor of somebody who is away, with his name and the day he is back on every row — and it is first because it is the only group on that screen about today: the rest have been waiting for days and will still be there tomorrow, and a customer expecting a call this morning will not. And his own day says it to him, with the one sentence that matters, which is that what is due is on the manager's screen until he is back. What did NOT change is his bands. A customer promised a call on Tuesday is still waiting whether or not the rep was at work, and turning that green on his return would be a comfortable lie told to the only person who can fix it. Away is his OWN leave and only while the office is open: on a Friday, a Saturday or a company holiday nobody is away, because nobody is there to be missed and there is no floor for anybody else to stand on — which is also why the rule is a pure function with a spec of its own, since the screens that show it can only be walked five days in seven and a suite that only walked them would prove nothing two days a week, silently. The demo now has somebody away today whichever day it is run: leave was seeded a week ahead from the first day it existed, so the pace path that knows about leave, the report's own "off" mark and both of these new places had never once been seen with anything in them. DEFAULT — founder may change.

- D76 A revision says what it changed. Item 10 on the phase-9A list, and six words in the coordinator's day: she re-issues blind. She prices Q-12, issues it, and a week later Q-12/2 arrives carrying the same three lines with nothing on it to say which of the nine fields on which line moved — so she prices all of it again, or opens the old one in another tab and compares by eye, on every revision this business raises. The drawer carries a block above the lines now: what changed from the quotation this one was raised on, one entry per line that is not as it was, the new value first and the old one after it. No arrow between them — an arrow is a left-to-right glyph on a screen read both ways — so it is said in words: "138.00 — was 134.00". Compared against the quotation the revision NAMES as its parent rather than against the newest older one, because what the rep had in front of him when he pressed Revise is what his changes are changes to. And compared on the WORDS rather than the ids: supplier N and class A2G1 are what she reads, and a line whose class id moved while its name did not is not a change to anybody looking at it. The difficulty is not the comparison, it is the pairing. A revision opens on its parent's lines in order, so position is nearly always right — and a rep who deletes the second of four lines would then be told that lines 2 and 3 changed everything and line 4 was removed, three lies for one deletion, every one of them believable on screen. Lines pair in three passes, strongest first: identical lines, then lines carrying the same colour code, then whatever is left, in order; and what is left over on one side was added or removed. A removed line is named by its colour rather than by its number, because its number belongs to the document she is not looking at. "Nothing changed on the lines" is printed rather than hidden, because a revision with the same lines is a real thing and a silent panel reads as one that failed to load. It is a pure function with a spec of its own, for the reason D75 gave: the demo has one revision in it with two lines repriced, and everything a rep will do in his first week — delete the middle line, quote one colour at two thicknesses, raise a revision that changes nothing — is in the spec and in no dataset. DEFAULT — founder may change.

- D77 The app says whether it is being used. Item 12 on the phase-9A list, last of the twelve and the one that decides whether the other eleven matter: adoption is what kills a CRM, the admin's screen said nothing about it, and a rep who stops opening Kladra does not appear as a rep who stopped — he appears as a rep with no follow-ups, no log entries and a quiet month, which reads like a slow week. Two facts per person and no third. **Opened** is the last Riyadh day that person had a screen answer them, written by the one query every authenticated request already makes: a day rather than an instant, because the question is who has not opened it this week and nobody acts on the hour — which also makes the write one UPDATE per person per day and none for the rest of it. **Changed this week** is how many records they wrote, counted from `audit_log`, because every write in this app already puts a row there with who and when: one definition asked of every role rather than a different verb per job, so a rep logging calls, the coordinator issuing quotations and marketing adding companies are the same question asked once. No score, no ranking, no percentage (S46) — the admin reads two columns and does one of two things: ring the person who has not opened it, or ask the person who opens it and changes nothing what is in his way. Somebody on leave is not marked amber, because he is not avoiding it, he is not at work (D75). It is the admin's screen and not the manager's: the manager's is about metres, and one table cannot answer two questions. The seed's stale account is the MANAGER, nine days out, which is the only one of the seven for whom it is true — every rep here logged a call two days ago, and a manager writes nothing at all because he reads, so a manager who stops opening it leaves no other trace anywhere. It is also the worst case there is: the screen that replaced his spreadsheet is the one he stopped looking at. And the seed now writes the audit row that each log entry and each daily report would have left, for the same reason the quotation trail does (D72): a dataset that inserts rows without them shows a floor where nobody has done anything. DEFAULT — founder may change.

- D78 A person's floor has a standing strip of its own. The half-built one the five days named: a company has answered “how is this going” since P8.5 and so has a project, and the person between them never did. The manager presses a name on the team screen and lands on that rep's floor — his month at the top, his customers underneath — with everything about what is actually IN PLAY left behind on the screen he came from, in the row he had just pressed. Three figures now, between the month above them and the calls due below them. **Pipeline**, what is still out there to win. **Open quotations**, what is in play, with the part of it the customer is holding said in the caption beside it — because that part is the one nobody here can move by working harder, and a figure without the reading beside it is the note Jerom wrote in 9C. **Sent back or refused**, what has stopped and stopped on HIM: amber, and the same amber the badge on his own day wears for the same rows. Those two counts come from `waitingOnRep`, the list his day already renders, so the manager reading “2 stopped” and the rep reading two rows are reading the same two rather than one figure derived twice (rules/data.md). Every figure is scoped to the FLOOR and not to the account — the companies whose rep he is, never the rows he happens to have typed — which is what makes it the same question the company strip one level up asks, and why a floor somebody covered while he was away still reads as his. The same band renders on his own floor, for the reason the month card does: a rep should recognise the row the manager reads as the card on his own screen, and two layouts for one set of facts is how two readings of one figure begin (D59). Marketing gets none of it, which is the sentence that already takes the month card off its day (D44) — it owns companies and raises no quotations, so two of the three figures would be nought on every screen for ever, and the follow-up strip below is its own standing. Building it turned up a second copy of “open quotations”: the team table had the three statuses and the live-revision test written out with the query builder while `src/lib/standing.ts` held the same definition in SQL — both correct, and exactly the pair that drifts the day somebody adds a status. There is one now, and the table reads it. DEFAULT — founder may change.

- D79 A notice is cleared by doing the work, and a finished one is cleared by reading it. The second half-built thing the five days named: notifications were created and never expired. Nothing in the app could ask whether a notice was still TRUE, because a notice held a sentence, a link and nothing else — so “Q-2 came back for edits” stayed in a rep's bell and on his screen after he had fixed it and asked again, and the only way to make it stop was to read it, which is the one thing that had nothing to do with the work. A notice says what it is ABOUT now, as a record rather than as a URL (`subject_type` and `subject_id`), and every kind answers one question the compiler asks of it: what takes this off the screen. **Work** — a request waiting on the coordinator, a quotation sent back to the rep, one issued and out with a customer, a dispatch waiting to be approved — is cleared by the transition that settles it, inside the same transaction that makes the change, so the bell and the row stop existing at the instant the thing they described stops being the case. That is S52 said about the bell instead of about a follow-up date: a reminder is cleared by doing the work. **A finished fact** — the customer accepted, the customer rejected, the request was withdrawn, the dispatch was approved or refused, a lead was handed over — has nothing at the end of it to do, so reading it IS acting on it and it is deleted when it is marked read rather than greyed and kept for ever. What is left on that screen after “Mark all read” is exactly the work still open, which is what makes it worth opening. Deleted rather than flagged, because a notice is a pointer and what actually happened is on the audit log, which is the record. Two things came out of the same slice. The seed had a notice saying Q-4 was issued against a quotation the customer had accepted two days earlier — the exact row this makes impossible — and it is the acceptance now, beside a sent-back notice for the request that really is sent back, so the demo shows both halves rather than neither. And the notification was the one screen in the app that kept a copy of a person's NAME: the actions stored `actor.name`, which is the Latin one, so Rawan read «طلب Faisal Al-Harbi عرض السعر» on the screen whose whole design is that the row holds no words and the sentence is built in the reader's language at read time (D13, D68). The row carries his id and the screen names him. DEFAULT — founder may change.

- D80 A screen says what it is not showing. Two of the three carried items, and one sentence under both: a list rendered every row it was given, and a form that scrolled looked exactly like one that ended at the fold. **The lists.** `listCompanies`, `listProjects`, `listQuotations`, `listDispatches`, the five groups of the stuck list and the four bands of a rep's day all had no LIMIT anywhere in them, and every row renders TWICE — a card for the phone and a row for the desk, one of them hidden by CSS at whichever width you are at. On the seeded floor that is twelve companies and nobody notices; on the founder's it is a thousand, and the row a rep needs is inside a megabyte of HTML he will never scroll to. Each list asks for what its screen will draw now (two hundred on a list screen, twenty-five in a band, twenty in a group), and where it is capped it says so in a line that carries both figures and what to do about the difference — the search box, which is how anybody finds one customer anyway. Not a pager: there is no page two in this app, because a rep looking for Al Noor types Al Noor (DESIGN §4). The figure above a list never counts the rows under it — `topOf` keeps the true total beside what it drew, and it is a pure function with a spec of its own, since a cap that bites at two hundred cannot be reached by walking a demo of twelve. **One list is deliberately NOT capped**: a company's own activity, because that history is the record S27 calls the manager's daily report, the CSV export does not carry it, and truncating the only copy of something is not the same as shortening a list you can search. If it ever gets long enough to hurt it gets a control, or the export gets activities. **The form.** Measured on the request dialog: one item fits, two do not, and the totals and the note to the coordinator sat below the fold with the footer still visible above them — so it read as though the form ended at Add item. Nothing was broken, which is the whole problem: Chromium's overlay scrollbar fades, and a surface with more below it looked identical to one without. `FormBody` carries a scroll hint now — four backgrounds and no JavaScript, two of them the dialog's own colour pinned to the content and two shadows pinned to the box, so where there is nothing to scroll the covers sit exactly on the shadows and hide them. One change, every dialog in the app. DEFAULT — founder may change.

- D81 A dispatch starts from the last one against its job. The third carried item, and the same complaint as D74 one screen along: the destination and the payment terms are free text on every dispatch, and the second one against a quotation goes to the same gate on the same terms as the first — typed again from nothing every time. The form opens on the last dispatch raised against that quotation now: where it is going, how it is paid for, how it ships, filled in and editable. Any status, deliberately — a refused one describes the same site, and a dispatch that came back is the likeliest reason a rep is raising another. What does NOT carry is the quantity, which is the whole of what this dispatch is and the one thing he came to the form to say; the boxes start empty however many the last one sent. It follows the QUOTATION rather than the company, because the quotation is the job: two jobs at one customer are two sites. And it is asked per open rather than handed down, for the reason D74 gives — on the Dispatches screen the quotation is picked inside the form, so the answer has to follow what he picked. It was NOT on the ranked list and no user named it: the five days found it at the quotation and the dispatch form has never been walked with a rep, so this is the founder's to keep or drop when he sees it. DEFAULT — founder may change.
- D82 One dialog per screen, and a long list drawn from data (P10d). The first walk at the founder's volume found nothing a person could see and one thing a phone would feel: the rep's day screen weighed a megabyte and took three times as long to render as the two-hundred-row customer list beside it, because every card on it carried its own copy of the log dialog and its own serialised copy of the card. Both are one thing now — the screen mounts one log form and every card presses it; the cards, the waiting rows and the manager's stuck rows are drawn by one component each from plain data. No screen reads differently, no word changed, and the specs that press Log from a card, from a drawer and from a history entry pass unchanged. Recorded here because it is a rule about how every list from now on is built (DESIGN §5), not because the founder needs to decide it. DEFAULT — founder may change.
- D83 The waiting list is capped like the bands under it (P10d). At the founder's volume Faisal's day opened on sixty-four "waiting on you" cards before the first call due — the calls, which are the day's work, buried under the list that sits above them. It draws the twenty-five longest-waiting now — ordered across all three sources by when each stopped, which the query had never done: it listed newest-first per source, so the cap would have hidden the oldest sent-back quotation first — with the true count in its heading and "and N more" under it, the same shape as the four bands (D80). The three sources are merged and ordered in memory and the page keeps the top of the list, rather than asking SQL for twenty-five: the order spans three tables, and the list is bounded by one rep's open work, not by the floor. The line is a count and not a link, unlike the bands': the rest are quotations sent back, quotations with the customer and dispatches refused — three statuses on two lists — and there is no one screen that shows exactly them. If a rep ever lives above this cap, a "waiting on me" filter on the quotations list is the honest next step and the founder's call. The manager's strip on the rep's floor (D78) counts all of them, not the twenty-five drawn. And a fifth figure joined the manager's own strip: "gone quiet" was listed below with no number above it, and at volume it was the largest group on the screen — 249 against 38 requests waiting. DEFAULT — founder may change.
- D84 A form with something typed in it does not close on a tap beside it (P10d, from the design-guidelines pass). The log dialog is filled in standing up, on a phone, and a thumb landing on the dimmed page behind the sheet used to throw the sentence away. Once anything is changed — a word, a chip, a date, a pick — a tap outside does nothing; Cancel and Escape are deliberate and still close it, and nothing asks "are you sure" — the app has no confirm on a blank form and this adds none. The same pass asked for a focus move to the refused box on Save, which it now does, and for the three-hundred-entry history to be virtualised, which it is not: it renders in fifty milliseconds in production and the drawer scrolls, and a history that ever needs more should be cut by year, in words, not by a scrolling window. DEFAULT — founder may change.
- D85 A write holds its row before it decides (P11A, findings 1, 2, 6 and 57 of the stranger read). Every transition in the quotation and dispatch chain checked the state it expected BEFORE its transaction and then updated by id alone — two people each reading "waiting", each writing an answer, the second silently overwriting the first — and the quantity check on a dispatch was the same shape one step out: a plain SELECT under READ COMMITTED, so two reps pressing Save in the same second could both spend the last panel while the comment above it said the transaction made that impossible. Every such write now begins by holding its row (SELECT … FOR UPDATE) and asks the state inside the hold; a second hand that acted first gets the sentence the app already had for that state — "This request is not waiting any more", "already been answered" — and nothing else is written. A dispatch request holds the quotation row, so two requests run one after the other and the second sees the first's panels gone; approving a dispatch asks, inside the hold, whether its quotation is still the live revision (D36 had asked only when the dispatch was raised) and refuses with the sentence that already existed for a superseded one; and two revisions raised at once take consecutive numbers rather than colliding on the unique index. No new words, no new screens: the change is that the door is locked while the check at it runs. The spec that presses Save in two tabs found, on its first run, that the "still live" question had been written with a Drizzle column inside a correlated subquery and so compared the inner row with itself — the trap rules/data.md already names — and it is raw SQL with both tables written out now; a guard nobody has watched refuse is not yet a guard. The remaining finding under this heading — a stale row in the coordinator's queue does not yet show that its quotation was revised — is a screen, not a write, and waits for 11E. DEFAULT — founder may change.
- D86 Achieved metres belong to the rep who raised the dispatch, and a figure is written once (P11A, findings 3, 4, 46 and 52). "Achieved" joined the customer's CURRENT owner, so handing a company to somebody else moved every metre ever approved on it into the new person's months and out of the old one's — the history of a target rewritten by an act about the future. It follows `dispatches.rep_id` now, which the row has always carried: whoever raised the dispatch made the sale, and a hand-over moves the customer, his projects and his open work and nothing that was already approved. The same word on the same screen: a quotation or dispatch says "Raised by" rather than "Rep", because after a hand-over the two are different people and both are true, and the hand-over warning says what moves and what stays instead of claiming the metres move. And the square-metre formula — width × length × quantity sent, rounded per line and once over the sum — was written by hand in six files and two specs, every copy right and none held to another; it is `src/lib/sqm.ts` now and `npm run lint` refuses a seventh copy, while the specs keep theirs, because a figure computed two ways is what a spec is for. Pipeline stays with the current owner: it is about what is open, and D42 already says whose customer that is. DEFAULT — founder may change.
- D87 A terminal action records what it did, and only what it did (P11A, findings 7, 8 and 44). Three shapes of one fault. Archiving a company wrote a date and nothing else, while S16 promises the record shows why someone gave up on it and every other terminal state here — lost, sent back, refused — carries its reason: the archive dialog asks why now and refuses a blank, the answer is kept on the company and in the audit line, and the admin's archive screen shows it under the name. Unfiling a log entry was offered on any day and checked nothing, while correcting one is allowed only inside the reporting window (D58, D70) — sixty lines apart in the same file, the two had drifted; unfile takes the same window now, in the action and on the button, so an entry from a reported day can no longer vanish from a figure the manager has read. And five of the admin's writes logged an audit row whether or not a row had changed; each writes its row only when the database handed one back, and says "not there any more" otherwise. The reason lives as long as the state it explains: restoring the company clears it, as a sent-back reason clears when the request is edited again. The refusal for a blank lands under the box, not only in a toast, which is the rule the reason dialogs already kept. The reason is free text and short; nobody asked for a list of reasons, and a list would be a guess at what a fourteen-person office says. DEFAULT — founder may change.
- D88 A typed key is refused by name and can be corrected (P11A, finding 5). The SMAC number is the one value the spec itself calls error-prone (S3): typed by a person, the only link to the system that holds the money, unique by index in both chains (D53). Typing one twice came back as "something went wrong", and a number typed wrong was wrong for ever — the two ways a unique typed key fails, and neither had an answer. A clash is now named at the field, "{number} is already on Q-12/2", and the coordinator can correct the number on an issued quotation or an approved dispatch: the row is held (D85), the status and the month do not move, the old number goes into the trail as "was …" (D72), and a number another record carries is refused the same way. Asking where else the fault lived found the contact form: drizzle wraps the driver's error and the duplicate-phone check read the wrong level, so "that phone is already on this company" had silently become "something went wrong" too; one helper reads the constraint name now (`src/lib/pg-errors.ts`) and both forms use it. Only the coordinator corrects, because only she types these; a rep who spots a wrong number tells her, as he would about the paper. DEFAULT — founder may change.
- D89 A phone is read in its company's country (P11A, finding 9). `normalizePhone` treated every number as Saudi: 050 123 4567 on a Dubai customer's card became +966 50…, a number nobody could ring, and the WhatsApp link went with it. Every caller had the country in scope — the form's country picker, the company row — and none passed it. Now each does: the add-company form reads the number in the country just picked and asks the duplicate check the same way; adding or editing a contact reads it in the company's country; the search box alone keeps Saudi, because there is no company yet. The table of country codes is the countries this business sells to, not the world; a number from a country not in it is accepted with its + and refused without one, which is the honest answer when the trunk prefix is unknown. The guidance beside the field follows: a Saudi company's field still suggests 05x and +966, and any other country's says "as it is on the card, or with its country code". The old fallback that turned any eight digits into an "international" number is gone. DEFAULT — founder may change.
- D90 The sentence over the six-month bars says what the bars say (P11A, finding 13). It said "the first month with anything in it" whenever the month before the last finished one was empty, over a row of bars that plainly had metres in earlier ones; and it said the same when the last month was empty too. It now distinguishes a first month from a month after an empty one from two empty months in a row, and only ever calls a month "first" when no finished month before it had anything. DEFAULT — founder may change.
- D91 A permission is a role and an id, never an id alone (P11A, finding 10). `mayWrite` asked only "is his id on the company?", so a rep promoted to coordinator or admin kept every company he had and could work them for ever from a role that has no floor; `mayQuote` beside it had asked the role too. Writing needs both now, and the admin cannot give a floorless role to an account that still holds companies — the form says how many and asks for a hand-over first, because a company left on a role with no floor is a company nobody can work. The floor test that expected every role to write on its own was changed with this, and says why. DEFAULT — founder may change.
- D92 A child comes back onto its company, never dragging the company back (P11A, finding 11). Restoring an archived contact or project un-archived its company as a side effect, so a company archived on purpose was on the floor again because of a stray contact, with nobody having decided that. If the company is archived, the archive screen shows "restore the company first" in place of the button, and the action refuses the same way; restored, the company's own children are still where they were archived, each with its own row on that screen. Every restore writes its audit row only when a row came back (D87). DEFAULT — founder may change.
- D93 `backup:verify` holds the restore to the counts taken with the dump (P11A, finding 12). It compared a restore of last night's dump with the live database now, so it failed on every day the business used Kladra and only ever passed on an idle one — a check that cannot pass on a working day is a check nobody runs. `npm run backup` now reads every table's count just before and just after `pg_dump` and writes both beside the dump; verify compares with the after reading, and where the two readings differ — rows written while the dump ran — accepts anything between them for that table. A dump from before the record existed is compared with the live database as before, and says so. DEFAULT — founder may change.
- D94 A drawer says what it writes (P11A, findings 15, 16 and 21). Three places where the screen and the write disagreed. The company drawer raised a quotation against no project while S18 says every quotation belongs to one — now the drawer's request asks which of the company's open projects, offers no button when there is none (the sentence says to add a project first), and the action refuses a request without one. The follow-up picker at the top of the drawer writes the company's own date while the list colours the row by the earlier of that and its open projects' dates, so "cleared" could leave the row amber: the drawer now says which project is due when, and the toast says "cleared, {project} still has its own date" rather than pretending. And correcting or unfiling a log entry wrote its audit row and told nobody — the same transaction now sends the live event a new entry sends, so an open drawer and the day's counts move without a reload. DEFAULT — founder may change.
- D95 A figure agrees with the figures under it (P11A, findings 22, 23, 24). Three strips where the number and the words under it were two different reads. A rep's "open quotations" counted asked, sent back and with the customer while the caption named only the ones with the customer, so six over three left three unexplained — the caption now names all three parts from the same read as the figure, and the parts add up to it by construction. The Use screen's headline counted people its own rows excused as away — one predicate, `isQuiet`, now decides both the count and the amber. The queue's "longest wait" came from a second query that never asked whether the company was archived, so it could name a request neither list showed — it is read from the rows the page shows, as the counts beside it already were. The rule these share: a strip is read once, and a caption over a list comes from the list's own rows. DEFAULT — founder may change.
- D96 The guards (P11A, findings 14, 19, 25). Three doors the stranger found unlatched. The log sheet's guard against a tap beside it (D84) never saw the phone's back gesture, which is a route change: while the sheet is dirty, one history entry is pushed and going back lands on it — same screen, nothing lost — while Cancel, Escape and Save still close it and take the entry with them. A CSV cell that opens with `=`, `@`, a tab or a return, or with `+` or `-` in front of anything but a plain number, is written with a leading apostrophe so Excel reads it as text rather than running a formula somebody typed as a company name; phones keep their plus and negative figures stay figures. And the message check reads every computed-key family the screens render — eleven, not five — from the source lists themselves, so a member added to any of them without a word in both locales fails the build. DEFAULT — founder may change.
- D97 A day as it happened (P11A, findings 26, 30, 32). Three places where the calendar or the role was read wrong. A rep who worked a Saturday had no box to write in, because an off day showed none: the box is offered on an off day that is still open to him, says nothing is owed, and is never counted against him (S47, D57). Marketing's report card carried eight figures of which six — quotations raised, sent back, answered, dispatches raised, approved, metres moved — it can never touch (D50); its card carries the two it can. And the working-day age of a stuck request read the calendar from the first of this month (the manager's screen) or from a fixed sixty days back (the coordinator's), so a request raised before that window aged a holiday it crossed as a working day; both read the calendar back to the day the oldest request was raised, which is the day the count starts. DEFAULT — founder may change.
- D98 A number is a call (P11A, findings 29, 31, 39). "Calls due" offered WhatsApp and no way to place a call — `tel:` appeared nowhere — so a phone number on screen is now a message and a call everywhere it is drawn: the number is the WhatsApp link and stays readable, and the handset beside it dials; the list, the drawer's contacts and the day's cards draw the one shape (`PhoneLinks`). A call card with no contact and no number said nothing where the customer list says "No contact yet"; it says the same words now. And the prompt that takes a SMAC number showed a bare label — "Issue Q-12" — while the coordinator typed a number against a company she could not see; the prompts that take a number carry the customer's name under the title (issue, approve, and both corrections). DEFAULT — founder may change.
- D99 Derived from the source, walked in the spec (P11A, findings 36, 37, 38). The admin gate was two lines hand-copied into seven pages, and the spec that swept them was a third copy of the list that had missed one screen for three phases: the gate is one function (`requireAdmin`) and the sweeps read the rail's own list (`ADMIN_PATHS`). And two states the business has were in neither the seed nor the spec — a dispatch the desk refused, and a customer's rejection recorded live — so the walks WORKFLOW §3 called done had never been walked: the seed carries a refused dispatch with its reason and its audit trail (and every seeded dispatch its request and approval rows, D72), the spec refuses one and rejects one through the screens, and the check that a decided quotation carries its instant is tried against the database. Found on the way: the volume seed drew its SMAC numbers at random and collided with itself; it counts them now. DEFAULT — founder may change.
- D100 The database says what the code assumes (P11A, findings 42, 43, 55, 56). Four rules the code relied on and the database did not hold: the kind of record a notice is about was a closed list in TypeScript over a free-text column (now a check that reads the same list the type is derived from); the use panel's read of who did what in the window had no index by person and instant (now `audit_log_user_at_idx`); a quotation's lines had no uniqueness on their position while a dispatch's had (now `quotation_items_position_idx` — the app rewrites lines by delete-and-insert, so nothing renumbers against it); and a target's month was made the first of the month by the form only (now a check on both target tables). Each is 0002's argument again: the form is one way in, and the seed, a migration and next year's import are others. DEFAULT — founder may change.
- D101 What it says it is (P11A, findings 45, 58, 65, 67). The funnel's sent-back row said "never asked again" of a request sent back that morning: the words are now true of every row they count — "not asked again yet" — and the row says how old the oldest is, read off its own send-back in the trail. The log opened from a call card starts on the contact the card names; the rep can change it. A count of the archive that no screen ever read (`archivedCount`, kept for a badge never built) is deleted rather than kept. And the check for unused strings exempted all of `common` because one call built a key under it at runtime; a bare dynamic namespace now reaches exactly the members the source lists for it, and the families table both message checks read lives once (`scripts/lib/message-families.ts`). DEFAULT — founder may change.
- D102 One word for one thing (P11A, findings 48, 59, 60, and the Arabic reviewer's notes from P11A-11). The glossary is the list and both locales read it: a revision is مراجعة on every screen (it was نسخة on one dispatch sentence), a person is one word on the team screen and in admin (it was two), and a country is chosen, never picked — with a pass in `check:messages` that refuses pick and select in the English (it found a second one, on the project a quotation needs, the day it was written), because a glossary only people read is a glossary that drifts. And the months sentence has three answers: up, down, and matched — two equal months read "0% up" until the third sentence existed. DEFAULT — founder may change.
- D103 A test that cannot pass for nothing (P11A, findings 47, 49, 50, 62). Two specs could pass with nothing on the screen — an assertion behind an `if` the page decided, and a click made only when the button was there — and one re-implemented the app's day rules beside the app; and the message check three files call part of the build ran only when somebody remembered it. A test asserts for the rows it has decided exist, asserts a control's absence rather than maybe pressing it, imports the rule it checks against, and `check:messages` runs in `lint` and before every build. The honest version of the view-as test failed the same hour: "Mark all read" was offered while viewing, refused by the door but offered — it is hidden now, as every other control that cannot be used. DEFAULT — founder may change.
- D104 The volume floor has a past (P11A, finding 61 and what reading the seed showed). `seed:volume` wrote no audit rows at all, so at the one scale meant to be walked every trail panel was empty and the use panel called everybody quiet; and it broke the business in three quieter ways — a quotation's rep was not its company's, a dispatch could be raised before its quotation and by somebody else, and issued, decided and raised were one instant, so every age read zero. The volume floor now leaves the trail the actions leave (request, issue, send back, accept, reject; request, approve, refuse; a logged activity), obeys the floor (D86) and the calendar, and proves its own output before it says done — every quotation has the row it was born with (a request, or a revise for a revision, as the actions write them), every dispatch its request, no dispatch precedes its quotation: a seed that lies about what it wrote is the finding. The check bit on its first run, on a revision the demo seed had born as `revise`, which is right; the check learned the rule. DEFAULT — founder may change.
- D105 Live is proved, not promised (Phase 11B; findings 17, 18, 54, 63, 64). Two people see each other's writes with no reload, and the spec now does it: the rep raises, the desk's queue shows it and marks it as just arrived; the desk issues, the rep's list and bell change under him. Every list marks an arrived row (the quotations and dispatches lists, and so the queue, never did). A read is a read whoever is looking: the live channel and the bell's count open for a viewer as they do for the person (they were gated by the write door, so live went dark the moment anybody pressed "view as"). And a Postgres outage longer than one reconnect is not a hole: whoever connected while the listener was away is told to re-read when it returns, once, and the recipient list is cut into chunks that a synthetic audience of four hundred has now exercised. Writing the two-people spec found one more thing: the arrived highlight's two seconds started when the event landed, so the refresh that paints the row was subtracted from them — most of the two seconds on a slow route; the clock now starts when the list's rows have changed on screen, and the spec times the highlight from the moment the row is there. DEFAULT — founder may change.
- D106 Closed where the code is closed (Phase 11C, the sweep after D100). Three more columns the code treated as closed lists and the database took as free text: the kind of a notice (ten kinds, each a sentence in both locales), the kind of record a trail line is about (eighteen: the eight records of the floor, the two admin settings, and the eight reference lists, those read from their own list), and a company's archive reason, which is a fact about a company that has left the floor and now cannot sit on one that has not. Each list is written once, beside its column, and the check reads it; `notify.ts` re-exports the notice kinds so nothing that imported them moved. The demo now keeps one archived company with its reason, so the half of the archive rule that lets an archived company clear its reason is exercised rather than skipped, and the archive screen has a row on it before anybody archives one (rules/data.md). And the drift between the schema file and the database is itself tested: every check and index the schema names is in the catalogue, and nothing in the catalogue is unknown to the schema. DEFAULT — founder may change.
- D107 Reads measured at the volume floor (Phase 11C-2). With statement logging on, every role walked every screen of the production build against the eight-hundred-company floor: 653 statements over 45 screens, none over 13 ms, most screens under 10 ms of database time in all. The one plan that grew with the data — the projects list asking each project for its last activity by walking the whole activities table once per row — has its index now; every other per-row read already used one, and at this size the planner's whole-table scans of a few hundred rows are the right answer, not a missing index. What the measurement surfaced is not statement time but count: the manager's day asks forty-eight questions for one screen, and every screen reads the session row seven times over — parked for 11I as §5 #71. The baseline and the method are in WORKFLOW §3 for 11I to re-measure against. DEFAULT — founder may change.
- D108 A count counts the rows its list shows (Phase 11D, finding 72). The follow-up figure was one number for three screens: it counted dates — a company's own and each live project's — while the pill on Companies opened one row per company, the chip on Projects opened projects only, and the day's bands drew companies under that same total, so "and 1 more" could point at nobody. On the volume floor the Projects screen read 37 overdue above a list of one. D9 still holds — the strip counts both sources — but it counts them as what the list it opens will show: companies, by the soonest date on them or on any of their live projects, on Companies, the day and the team table; projects on Projects. Each count is the list's own filter, built by the same function with the same date expression, so the number and the rows under it cannot come apart; `tests/counts.spec.ts` puts one company late twice over and holds each screen's sentence to its row count. DEFAULT — founder may change.
- D109 A company that comes back is recognised (Phase 11D, finding 73). The archive exists so that a customer somebody gave up on is known when he resurfaces, and why (S16) — and the duplicate check never read it, so an archived company was typed in again as a stranger and the reason was shown to nobody. The check reads archived companies too now: the warning under the name or the number says the company was archived, on which day, whose it was, and carries the reason typed then, in its own direction; a live match says where the company is and when it was last worked, which is what a rep decides by without leaving the form. A live match still sorts ahead of an archived one, and nothing blocks the save (S15). `tests/known.spec.ts` types the seeded archived company's number and reads the day and the reason back. DEFAULT — founder may change.
- D110 A notice names the customer (Phase 11D, finding 74). Every quotation and dispatch notice named a number and nothing else — "Q-12 came back for edits" — and a rep with eight open quotations, or the coordinator with thirty, had to remember which customer Q-12 was before the sentence meant anything. The customer is in the sentence now, in both locales, resolved at read time from the notice's own subject (D79) the way the rep's name is resolved from his id (D68): a row written last year names him too, and a renamed company reads right. DEFAULT — founder may change.
- D111 A call card says why (Phase 11D, finding 75). The day's call card named the company, the contact, the city and the date, and not the reason the call was owed: the words of the last entry lived two presses away on the drawer's Activity tab, and the rep who wrote "wants 4 mm samples, follow up tomorrow" read tomorrow's card as a name and a date. The last entry's words ride with the card now — one line, in the writer's own direction, from the same entry the card's "last activity" already dates — so a rep in a lobby knows what he is calling about before he presses the number (S23, S26). DEFAULT — founder may change.
- D112 A dispatch line says what already went (Phase 11D, finding 76). A dispatch's lines read "sending" and "on the quotation" and nothing between them, so the coordinator approving the third partial dispatch — "40 of 100" — had to open the quotation and count its mini list to learn that 70 had already gone. Each line now reads sending, on the quotation, on other dispatches (waiting or approved, this one excluded) and left to send once this one is counted, from the one committed-quantity definition the rep's dialog already uses (D12); a refused or cancelled dispatch holds nothing, so its own share is not counted against the line. The action's lock (D85) is still what refuses an over-send; this is what lets her see one coming. DEFAULT — founder may change.
- D113 A day off the calendar is a span (Phase 11D, finding 77). The calendar took one day per dialog, six presses each with the person picked every time; Eid is four or five days twice a year and S48's own case is a rep back from two weeks off — eighty-four presses, or, likelier, leave that never got entered and a pace that said he was behind. The dialog takes a first day and a last day now, and the action writes one row per day in the span, skipping a day already on that calendar; the table, pace and leave read exactly what they read before, and the trail holds one row per day written, each naming the span it came from. Sixty-two days is the most one entry may cover. DEFAULT — founder may change.
- D114 Enter saves what a person typed (Phase 11D, finding 78). The coordinator's number box — her most frequent act — a target box, New project, Edit project and Mark lost each took a click where every other dialog took Enter, because they were written before the form shell and never moved onto it. A dialog with a text field is a form and Enter submits it; a multi-line reason keeps Enter as a new line and takes Ctrl/Cmd+Enter. DEFAULT — founder may change.
- D115 A form starts from what the app knows (Phase 11D, findings 79 and 80). A new month's target boxes opened empty with nothing on screen saying what last month was; each box now says it and one press keeps it, and the admin still presses Save, because a target is a number somebody agreed out loud. The log opened on "No contact" for a company with exactly one person, so naming him was two presses on every entry or the entry went unnamed; it opens on him now, and with several people still on nobody, because a guess would misname the call (D101 stays: a button that names a contact wins). The boxes stay one Save each — an admin who fixed one number and left the page has changed exactly one number. DEFAULT — founder may change.
- D116 A queue row names its person (Phase 11D, finding 81). The coordinator's rows carried the quotation, the customer, the project, the metres and the wait, and the rep who raised the request only in the sheet; the conversation about a request is with him (S54), so she opened each row to learn whom to write to. On the queue a row names him, in the reader's script (D68); on a rep's own list it does not repeat his own name to him. DEFAULT — founder may change.
- D117 A count is a door (Phase 11E, findings 83, 84 and 91). The team table's habit counts — open quotations, overdue follow-ups, never contacted — were bare numbers, and the manager who read "37" had to go and find the thirty-seven; the pace cell on the desk read "4 / 22" while the phone card and the month card said "4 of 22 working days"; and the pipeline figure carried ".00" on a screen where every other m² is whole. A count opens the rows it counted, with the same filter the pill on that floor uses (D108), so the number pressed and the list that opens are one definition; the open-quotations count opens the floor whose strip carries the figure and its three parts (D95); a zero is plain text. The pace cell says its unit in words on every width. A sum of estimates is whole metres; a quotation's or a dispatch's m² keeps its decimals, because those were measured. DEFAULT — founder may change.
- D118 Stopped work comes first, and the heading says of what kind (Phase 11E, finding 85). The day's waiting list sorted its three kinds together by age, so on the volume floor a quotation a customer had held for a month sat above yesterday's send-back that had the coordinator blocked, and the heading said 83 over twenty-five cards with nothing saying where the other fifty-eight were or that most were customers thinking. The list sorts sent back, then refused, then with the customer, oldest first within each; the heading carries the three counts as pills, each a door to that kind's own list under its status, zero plain. DEFAULT — founder may change.
- D119 What the action will refuse, the screen says first (Phase 11E, finding 86, the leftover of §5 #2). Approval refuses a dispatch whose quotation has a later revision (D85), and the coordinator learned it from the refusal: the queue row and the sheet said nothing and Approve sat there as if it would work. The row says the quotation was revised since, the sheet says the sentence the action would refuse with, and Approve is disabled — refusing is still hers. DEFAULT — founder may change.
- D120 A figure says its window and counts its nouns (Phase 11E, findings 87, 88, 89 and 92). "Changed this week" sat over a figure that counts the trailing seven days, which on a Wednesday is not this week; the chain card read "Of the 1 raised … each one" and "1 of them reached a customer, and 0 came back"; the report's moved line read "1 Companies" and its biggest figure was called "Moved" over the m² of dispatches approved (S43's own definition of achievement). The window is written from the one constant the figure counts by, with a plural in both locales; a noun after a number is counted in both locales; the m² figure is "m² approved". DEFAULT — founder may change.
- D121 A warning that names a record is a door to it (Phase 11F, the 11D leftover). The duplicate warning under the Add company form named the company it matched and offered no way to look at it without abandoning the half-typed form, and "is this the same firm" is decided by reading the record, not by remembering it. Where the match is his own company, the warning carries "Open {name}": the drawer opens over the form, the form stays open and full behind it, and closing the drawer returns to it. Where it is another rep's, there is no door — a rep reads only his own floor (S8) — and the warning has already named who has it, which is the person to ring. DEFAULT — founder may change.
- D122 A screen that cannot draw itself is still Kladra's screen (Phase 11G, §5 #20). There was no error boundary anywhere and no not-found page: a query that failed, or an old link, handed the reader Next's own page, in Next's face, in English. One card now wears both faces — the screen that threw, with Try again and Home; the address that names no screen, with Home — inside the shell, with the rail still there, in the reader's language and theme. A second boundary above the shell answers when the shell itself cannot render, and a last one above the root layout, static and bilingual like the offline splash, answers when nothing else can. No digest, status code or id is ever on the card; the cause is in the server log. Home is a full document load: after a failure the client tree is the thing in doubt. DEFAULT — founder may change.
- D123 A look is asked for by name, and the current thing is a wash (Phase 11G, §5 #34, #66). `Button` and `Badge` had a `default` variant — a flat brand red no screen had chosen — and six dialog footers and the report box wore it because nobody had said `brand`. The default is gone and `variant` is required at every site, so a missing name is a type error rather than a second primary look. The board's open card was marked with a ring in the same red DESIGN §1 had retired from every surface; it is the `bg-surface-2` wash the lists give their open row. Dialogs zoom in 150 ms and drawers slide in 200 ms, inside DESIGN §2's band; menus, popovers and selects keep 100 ms, because a menu is not a dialog. The arrived flash starts from the amber text colour at a third rather than the 14% tint, which on the queue's dark rows was barely a change (P11B-1). DEFAULT — founder may change.
- D124 What the server can decide, it decides before the first byte (Phase 11G, §5 #40, #41). The rail's width lived in localStorage, which the server cannot see, so every load of a collapsed rail began expanded and snapped shut a frame later; it is a cookie the signed-in layout reads, and the first paint is the saved width. The browser's own chrome — the phone's address bar, an installed window's title bar — followed the operating system's colour scheme rather than Kladra's theme, so a light Kladra on a dark phone had a black bar; `generateViewport` reads the theme cookie and answers with the theme's canvas, named once for the layout and the manifest. DEFAULT — founder may change.
- D125 The offline splash wears the reader's theme (Phase 11G, §5 #53). The splash was always dark. It has no server to ask, so the theme cookie is no longer httpOnly — there is nothing in it to protect — and an inline script paints light when the cookie says so, before the first paint. The service worker's cache is bumped so an old splash cannot outlive the deploy. DEFAULT — founder may change.
- D126 A pressed link says it is working (Phase 11G). A row that opens a drawer, a filter chip, a view switch, a board card and a follow-up pill are all navigations to a search parameter, and between the press and the streamed answer nothing on the page changed — on a slow phone, long enough to press again. `LinkPending`, inside the link, shows a small mark after 150 ms and takes it away when the answer lands; a fast answer shows nothing. It is the one loop the app allows itself, because it says "working" rather than decorating; under reduced motion the loop stops and the glyph stays. The sign-out item goes quiet while its request is on its way. DEFAULT — founder may change.
- D127 Empty says why (Phase 11G). A picker whose search matched nothing said "No projects to quote on yet" to a rep whose projects were all still there; it says "Nothing matched" now, and the caller's sentence is kept for the case it names, no options at all. A board with nothing on it was six columns saying "Nothing here"; it is the list's own sentence, with its Clear. The queue said "the desk is clear" under a search that had simply missed, and offered an "All" that led back to the same screen; the clear-desk sentence needs no search term, and a status the page fixed offers no door. A manager reading a rep's floor lost `?rep=` the moment he typed a letter, opened a row or took the way back from an empty list; every /companies URL the screen builds carries it. The admin's lookups list said nothing when a kind had no rows. DEFAULT — founder may change.
- D128 One line between a phone and everything else (Phase 11H, §5 #35). The phone breakpoint was written three times — 640 in the responsive dialog, 639 in the company drawer, `max-sm:` in the log dialog — while the shell changed at Tailwind's `md`, 768; between 640 and 767 a tablet had a bottom bar under centred dialogs. The line is `md`, named once in `src/lib/breakpoint.ts` in the words Tailwind compiles to — `(width < 48rem)`, not `(max-width: 767px)`, which disagrees with it at a fractional width — and read by one hook, `useIsPhone`; the stylesheet says `md:`/`max-md:`, which is the same line; the lint refuses any other width query in src, and in globals.css only the blur line at 980px. A phone is everything narrower than 48rem. DEFAULT — founder may change.
- D129 Every form is a bottom sheet on a phone, and its primary action is the lowest button (Phase 11H, §5 #28). Six forms called the raw dialog — new and edit project, mark lost, the log, confirm and prompt — and stayed centred boxes on a phone, with the kit's footer order putting Cancel nearest the thumb; all of them come through the one responsive dialog now, Save lowest and a thumb tall, the footer padded for the home indicator. The sheet slides in 250 ms, the top of DESIGN's band, not the library's 500, and stands 88dvh tall like the company drawer. Anything typed into any form sheet holds it — no drag away, no closing on a stray tap, and the handle that invites the swipe goes — while Cancel and Escape still close it (D84); the log also counts its chips, dates and picks. The Add project sheet names its company in the title, because on a phone the sheet covers the drawer that said whose it was. The six take the one dialog width the other nine already had, 32rem, where they were 28. The button that opens a form stands outside the two faces: inside them it was replaced under the finger while the page settled on which face to wear, and a press in that moment opened nothing; focus comes back to it on close. The search palette is the one dialog that is not a form and stays a dialog. DEFAULT — founder may change.
- D130 A thing a thumb presses is 44px on a phone (Phase 11H, §5 #27, #51). The rule lived in the bottom bar and, by hand, on the bell; buttons topped out at 32 and the day card's Log and phone chips sat at 28 over a whole-card link, so a rushed thumb opened the drawer instead. One utility, `touch`, carried by the kit — button, input, select trigger and item, tab list, menu and list items — and by the number, the handset, the follow-up and waiting pills and the search box's clear: 44 by 44 at least below `md`, nothing changed above it. The glyph may stay small; the hit area is not. DEFAULT — founder may change.
- D131 A read is asked once per request, and what every screen asks is counted by a script (Phase 11I, §5 #71). Who is signed in was read from the database by the layout, the page and every figure under it — a session row and a user row each time — and the non-working days were read once per band; React's per-request `cache` wraps both, so the first answer serves the request. `npm run measure:reads` is the 11C measurement made repeatable: statement logging on for the run, one marker per screen, the container's log cut at the markers, forty-three screens across four roles including one open drawer per list; it counts the server's rendering of the document and nothing the browser asks for afterwards. `scripts/reads.baseline.json` holds the count per screen and `--check` refuses a screen that asks more without a sentence in WORKFLOW §3 saying why. It needs the production build and Docker, so it is run before a commit that touches a read, not in the gate. DEFAULT — founder may change.
- D132 A write whose request never reached the server is a sentence, and the words stay (Phase 11I). Every server action guards its own work and answers with the app's sentence when it fails; what none of them could guard was the wire — no signal in a lobby, a deploy in the second Save was pressed. That call rejects, and React hands the rejection to the error boundary — from a form's own action as much as from a button's: the whole screen became the error card with the rep's sentence still inside the form it replaced. One guard, `useWireGuard`, now wraps every call the client makes to a server action — twenty-three calls in twenty files, the sign-in included — and turns the rejection into a refusal that says "Could not reach the server, so nothing was saved" wherever that form already says its refusals — the footer, a toast — with every field as it was and Save alive; the next press saves it. A lint rule refuses a bare call, and `tests/unhappy.spec.ts` cuts the wire under a log, a company form and a confirmation. DEFAULT — founder may change.
- D133 First paint on a mid phone is a number with a ceiling, and the fourth font weight went (Phase 11I). `npm run measure:speed` loads the main screens of the three floor roles on a slowed Chromium — the CPU four times slower, the network a slow 4G, a 375-by-812 phone — signed in but with nothing cached, then once more warm, and reads what the page itself records: first paint, largest paint, the moment React is live (the same `html[data-hydrated]` mark the suite waits for) and the bytes that crossed the wire. Measured on 7 Sep 2026 against the production build: a cold screen paints in 1.2–1.6 s, is live in 2.9–3.5 s and costs 467–582 kB, of which 237–308 kB is script and about 160 kB fonts and styles; warm, it is live in under half a second for 44–86 kB. `scripts/speed.baseline.json` holds the cold figures and `--check` refuses a screen that got a fifth slower or a tenth heavier. The one thing the figures pointed at that cost nothing to fix was a font: four weights of Plex were shipped and the boldest was used once, on the brand mark, so it is three weights now, one file fewer per family on every cold load, and every cold screen 35–40 kB lighter and about 300 ms sooner live. What they pointed at that is not fixed is recorded in WORKFLOW §5. DEFAULT — founder may change.
- D134 A write pressed twice is one write (Phase 11I). The wire can lose the answer after the row has landed — the rep is told the server could not be reached and nothing was saved (D132), and presses Save again. The creating writes — a log, a company, a project — answer the second press with the row that already exists when the same person sent the same words to the same record inside two minutes; the audit line and the live update were written the first time and are not written twice. It has to be the same write, not merely a similar one: every field the form stores is compared, because a rep told nothing was saved is as likely to fix what he had got wrong before pressing again — the project, the day, a digit of the phone — and a match that ignored the correction would swallow it and say it was saved. An entry taken off the floor is not a twin either: unfiling and writing it again is how a wrong day is fixed. Two minutes is the window: long enough for the second press after the sentence, short enough that a second visit logged in the same words an hour later is its own entry. `tests/unhappy.spec.ts` lets the request through, drops the answer, and counts one row after the second press. DEFAULT — founder may change.
- D135 A session that has ended is said as such (Phase 11I). A rep whose sign-in expired over a long lunch and who then pressed Save was told "you are not allowed to do that" — the sentence for a real refusal, which reads as an accusation. The refusal now says the session ended and nothing was saved, and that signing in again in a new tab and pressing Save here once more will land it; the words stay in the form, because signing in on this tab would throw them away. DEFAULT — founder may change.
- D136 What a rep wrote about a customer is read back where that customer is read (Phase 11J). Notes were asked for in three places — on the company, inside the contact captured with it, and on the project (§3) — and were written, kept, queried and rendered on no screen at all: the only way back to his own sentence was the Edit dialog it had been typed in, which on a phone is three presses and a form, so the answer was to stop writing them. Each note is now read where its record is read: the company's and the project's under the action row of their drawer, in a box with the word "Notes" on it, clamped to four lines with Edit as the way to the whole; the contact's as a line on the contact's own card. A manager reading a floor he may not write on reads them too, because a note is what the record says and not something to press (D42). It is not a comment (the Never list): nobody answers it, nothing threads off it, it has no author line. DEFAULT — founder may change.
- D137 A screen has one search box, and the coordinator's desk is in the order she works it (Phase 11J). Four list screens searched and there were four boxes: the companies screen used a component and the projects, quotations and dispatches tables each drew their own inside themselves — the same debounce, the same clear, the same three bugs to fix three times, which is how the clear button grew over the text on exactly three of them (§5 #121). The queue is the case that made it a defect rather than duplication: it renders two of those tables, so it had two boxes, both writing `?q=` and each writing its own `?status=` over the other's, and the second one sat empty over a list that was already filtered because a box that holds its own text never hears what another box typed. Rawan typed the customer's name twice and believed the wrong box. Fix: one `ListSearch`, and the queue draws it once over both lists. And her two lists came back newest first while the desk's own caption has said "the longest wait" since P8: the row she must answer next was the last one she read. Both are ordered oldest first now — the same reasoning as the rep's waiting list (D83) — so the cap drops the newest rather than the oldest. DEFAULT — founder may change.

## §5 One word per thing

The word on the left is the only one the screens use, in both languages. A
second word for the same thing is how two people end up describing the same
screen differently, and how a rep learns one name and reads another.

| On screen | بالعربية | What it is |
| --- | --- | --- |
| Company | الشركة | The organisation that buys. §2 S11 calls it the customer; the screens never do. |
| Contact | جهة الاتصال | A person at a company. Belongs to one company (S11). |
| Main contact | جهة الاتصال الرئيسية | The one contact a rep reaches first. One per company (D18). |
| Project | المشروع | A job at a company — a tower, a villa, a mall (S18). Never a "job". |
| Quotation | عرض السعر | Never a "quote". The phone's bottom bar shows **Quotes / العروض**, the only short form in the app, because five tabs do not fit across 375px. |
| Dispatch | التوريد | Goods moving against a quotation (S37). Not the same as **Shipment method / طريقة الشحن**, which is how they travel (S40). |
| Request | الطلب | What a rep raises and the coordinator answers, for both chains (S28, S39). The screens say request, never "raise". |
| Log entry | تسجيل النشاط | One thing that happened, in the rep's own words (S24). The word "record" is kept for the system of record (S3). |
| Follow-up | المتابعة | The next date something is owed on a company or a project (D9). |
| Never contacted | دون تواصل | Added, no activity ever, older than fourteen days (S51). |
| Gone quiet | انقطع التواصل | Contacted at least once, no follow-up anywhere on it, nothing logged for fourteen days (D63). The pair to the row above: one says contact never started, the other that it broke off. Never صامت, which would say the customer chose the silence — what is measured is our own logging. |
| Lead source | مصدر العميل | How this company first reached Technopanel. |
| Archive | أرشفة | Takes a row off the floor and keeps its history (S16, D24). Nothing is deleted. |
| Mark lost | تسجيل كخاسر | Closes a project with a written reason (S20). Not archiving, and not a rejected quotation (D11). |
| Add | إضافة | The word for creating any record: Add company, Add contact, Add project. Never "New". |
| Choose | اختيار | The word for taking a value from a list. Never "pick" or "select". |
| Person | الشخص | Anybody with a Kladra account — rep, marketing, coordinator, manager, admin. Never الموظف: that is a payroll word, and the team screen is not a payroll list — Jerom's own row is on it. One word on the team screen, in admin, and in the hand-over dialog (D102). The admin's list of accounts stays **Users / المستخدمون**, one screen, one word. DEFAULT — founder may change. |
| Revision | المراجعة | A second version of an issued quotation, numbered Q-12/2 (S32). The first stays readable and is marked **Superseded / مستبدَل**. |
| Withdraw | سحب الطلب | The rep takes back his own request before it is answered (D32). Never "cancel" — a cancelled quotation sounds like a lost job. |
| Customer accepted / rejected | قبول العميل / رفض العميل | The customer's answer, recorded on the quotation (§3). The only place the screens say **customer**: everywhere else the buyer is the **Company**, and this is the one sentence where a rep is reporting what a person said. A rejected quotation is not a lost project (D11). |
| Approve | اعتماد | What the coordinator does to a dispatch request with SMAC's dispatch number (S39). It is the only event that counts toward a month (S41). |
| Refuse | رفض | Her other answer, with a written reason. A refused request gives its quantities back to the quotation (D12). Not the same as **Send back / إعادة للتعديل**, which is the quotation chain and expects the rep to fix it and ask again. |
| Left to send | المتبقي للإرسال | What a quotation line still has on it: quoted, minus everything on a dispatch that is waiting or approved (D12). |
| Pipeline | إجمالي الفرص | The sum of the expected m² on live projects (S45). Not a total of quotations, and not money. |
| Lookups | القوائم المرجعية | The reference lists an admin edits — cities, categories, suppliers, thicknesses. Never القوائم alone: the phone's bottom bar already says **Menu / القائمة**, and the two are one letter apart. |
| Daily report | التقرير اليومي | The one line a person adds to a day the system has already assembled (D55). Never a "log" — a **Log entry** is one thing that happened with a customer, and a report is a whole day. The phone's bottom bar shows **Report / التقرير**. |
| Rep | المندوب | §1. |
| Marketing | التسويق | §1, D50. The role, and the account that holds it. |
| Hand over | تسليم | Move a company to another person's floor (D51). Never نقل, which is what a shipment does. |
| Coordinator | منسقة المبيعات | §1 — Rawan. |
| Sales manager | مدير المبيعات | §1 — Abdulrahman. |
| Admin | مدير النظام | §1 — Jerom. |
