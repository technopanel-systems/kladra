VIDEO: MHyE3LlSGPs "5 mai 2026" (https://www.youtube.com/shorts/MHyE3LlSGPs) — REPEAT OF error-states.
Same source video as patterns/error-states.txt: identical "4 errors, 4 patterns" grid (validation/network/server/permission), identical severity→surface bar (Inline=Recoverable / Toast=Transient / Modal=Blocking, "Most apps overuse modals"), identical 500-modal-for-a-typo before/after, identical "Inline validation kills 80% of errors" live password-checklist demo, and the same closing 5-rules checklist (match type to surface / always offer a way out / severity drives surface / write copy for humans / prevent with live validation).
ADDS: nothing substantive beyond patterns/error-states.txt — the one extra concrete detail is that the Toast tier is illustrated with an inbox/notification list (not just a color swatch) and the Modal tier with a "Delete account?" confirmation dialog (Cancel/Delete), giving a slightly fuller worked example of the three severity tiers than the existing note's abstract bar-chart description.
