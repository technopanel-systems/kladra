VIDEO: g7hHuiTTiUc "UI/UX tips" (https://www.youtube.com/shorts/g7hHuiTTiUc) — REPEAT OF visual-hierarchy.
Same five rules, same worked examples (Monthly Revenue headline+bar-chart, rainbow-nav vs one-accent-nav and stat cards, Premium Plan contrast tokens, Start Free Trial whitespace, Monthly Revenue Report weight scale, closing "Five rules. One clear hierarchy.") as the existing pattern page — reads as the same source video already captured in patterns/visual-hierarchy.txt.
ADDS: one concrete number not already in patterns/visual-hierarchy.txt — narration states "double the padding around your important elements" for the whitespace rule (the existing note only has "generous breathing room" / "not empty, intentional," no multiplier); also narration phrases the size rule as "twice the size... not 20% bigger, twice," slightly sharper than the existing note's "roughly 2x."
